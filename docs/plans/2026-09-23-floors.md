# Этап 3a. Этажи башни, люк, буфет, вентиляция, сохранения v3 — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Разметить башню на этажи по 150 м с люком на потолке (пробивается массой ≥ M, иначе отскок; через 8 с под люком — вентиляция без награды), буфетом перед люком, стартом с любого открытого этажа, строками этажа в HUD и на результатах, сохранениями v3 и заголовком результатов «Шлёп!».

**Architecture:** Новый модуль `src/floors.js` между `world.js` и `game.js`: константы этажей, список люков, буфет, `hatchUpdate(dt)` → событие; `world.js` получает хук `spawnFilter`; `game.js` вызывает `spawnHatches()`/`hatchUpdate()` и применяет тряску, бонус и прогресс; `render.js` рисует люки и строку этажа; `screens.js`/`main.js` — селектор этажа на титуле и строки результатов; `save.js` — v3 (`floor`, `startFloor`, `skin`); `i18n.js` — `T(key, n)` с подстановкой `{n}`. Плоские скрипты, общий лексический scope, `expose()` для тестовых хуков.

**Tech Stack:** Vanilla JS, Canvas 2D, Node 22 для тестов (`tools/_env.js`), node-canvas для скриншотов.

**Spec:** `docs/specs/2026-09-23-floors-skins-design.md` (§3, §4, §7, §8, §9, §10, §11, §12 — часть 3a). Скины и реплики — отдельный план 3b.

## Global Constraints

- `FLOOR_H = 150` м; `floorOf(h) = floor(h / 150) + 1`; `hatchY(n) = -1500·n`; `floorBaseY(n) = -1500·(n−1)`; `startMass(n) = n === 1 ? 1 : min(1 + n, 12)`; `hatchMass(n) = min(3 + n, 15)`; `hatchBonus(n) = 5 + 5·n`; `BUFFET_M = 20`; `VENT_TIME = 8`; `VENT_NEAR = 300` px. Числа черновые, живут только в `src/floors.js`.
- Порядок скриптов: `core, i18n, audio, sdk, save, upgrades, ball, world, floors, game, render, screens, main`. Зависимости только вниз: `world.js` не знает про этажи (хук `spawnFilter`), `floors.js` не пишет состояние `game.js` (возвращает событие).
- Пробитие: `ball.vy < 0`, `ball.y − ball.r ≤ hatch.y + 12`, `ball.mass ≥ hatch.need` → `broken`, взрыв, `sfx.big`, событие `break`; `game.js`: `camShake = 12`, `runCoins += hatchBonus`, всплывающий `+N`, `floorCleared(n)` → `clearedThisRun`, при `n > save.floor` — `save.floor = n`, `newUnlock = n + 1`, `persist()`. Отскок: `ball.y = hatch.y + 12 + ball.r`, `ball.vy = 200`, `ball.vx *= 0.7`, `squash(200)`, масса не меняется, `sfx.hit` и надпись `T('needMass', need)` не чаще раза в 0.5 с. Вентиляция: `underT` копится при `hatch.y < ball.y ≤ hatch.y + 300`, при `≥ 8` с — `vented`, тон `tone(300, 500, 0.3, 'triangle', 0.15)`, надпись `T('vented')`, никаких бонусов и `save.floor`.
- Буфет: при создании люка этажа n — `meat` на `(160, hatchY(n) + 120)`, `pasta` на `(320, hatchY(n) + 60)` с флагом `buffet: true`; обычный спавн пропускает ряды с `(h mod 150) > 130`.
- Сохранение v3 `{ v: 3, best, earned, spent, up, floor, startFloor, skin }`; `SKIN_ORDER = ['none', 'chef', 'glasses', 'crown', 'bow', 'mustache']`; `startFloor` всегда в `[1, floor + 1]`; слияние: `floor`/`startFloor` максимум (с зажимом), `skin` — больший индекс в `SKIN_ORDER`.
- Строки ru/en/tr: `floor` «Этаж/Floor/Kat», `massAtLeast` «масса ≥ {n}/mass ≥ {n}/kütle ≥ {n}», `needMass` «Нужна масса {n}/Need mass {n}/Kütle {n} gerek», `floorCleared` «Этаж {n} пройден!/Floor {n} cleared!/Kat {n} geçildi!», `floorUnlocked` «Открыт этаж {n}/Floor {n} unlocked/Kat {n} açıldı», `vented` «Вентиляция/Vent/Havalandırma», `fell` «Шлёп!/Splat!/Pat!». В русских строках о Тефе — без гендерных форм.
- Титул: селектор этажа на `H·0.57` (кнопки `floor-`/`floor+` 44×44 на `W/2 ∓ 100`, текст `T('floor') + ' ' + save.startFloor`), виден только при `save.floor ≥ 1`; кнопка `shop` на `H·0.64`; `tapToJump` на `H·0.75`. HUD: `T('floor') + ' ' + floorOf(max(0, −ball.y/10))` на `(16, 66)`. Результаты: строки `T('floor') + ' ' + floorReached` и при `newUnlock` — `T('floorUnlocked', newUnlock)`.
- Каждый модуль заканчивается `expose({...})`. Никаких `TBD`/`TODO`. Коммиты на русском с трейлером `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Вывод команд в отчётах полностью или с явной пометкой `…(N строк опущено)`.
- Ожидаемый шум тестов: два блока `YaGames.init failed, using stub …` из `test_sdk.js`.

## Карта файлов

| Файл | Ответственность в 3a |
|---|---|
| `src/save.js` | v3: `floor`, `startFloor`, `skin`, `SKIN_ORDER`, миграция, слияние, снимок |
| `src/i18n.js` | `T(key, n)`, строки этажей и люка, `fell` |
| `src/floors.js` (новый) | константы, `floorOf`, `startMass`, `hatchMass`, `hatchBonus`, `hatchY`, `floorBaseY`, `inBuffetZone`, `hatches`, `resetHatches(n)`, `spawnHatch(n)`, `spawnHatches()`, `pruneHatches()`, `activeHatch()`, `hatchUpdate(dt)`, `setStartFloor(n)`, `drawHatch(h)`, `spawnFilter` (зона буфета с запасом 3 м) |
| `src/world.js` | хук `spawnFilter` в `spawn()` |
| `src/game.js` | `reset(n)`, `floorReached`, `clearedThisRun`, `newUnlock`, `floorCleared`, вызовы `spawnHatches`/`hatchUpdate`/`pruneHatches` |
| `src/render.js` | строка этажа в HUD, `drawHatch` в `drawWorld` |
| `src/screens.js` | селектор этажа на титуле, строки результатов, сдвиг кнопок |
| `src/main.js` | `reset(save.startFloor)` в `boot`/`restart`, кнопки `floor-`/`floor+` |
| `index.html`, `package.json` | `src/floors.js` после `src/world.js`; `test_floors.js` в `npm test` |
| `tools/test_save.js`, `tools/test_floors.js` (новый), `tools/test_render.js`, `tools/smoke.js`, `tools/shot.js` | тесты и скриншоты |
| `README.md`, `docs/DEVLOG.md` | правила этажей, сохранения v3, план, запись сессии |

---

### Task 1: Сохранения v3, `T(key, n)`, строки этажей, «Шлёп!»

**Files:**
- Modify: `src/save.js`, `src/i18n.js`, `tools/test_save.js`, `tools/test_render.js` (две строки `Упала`/`Fell`)

**Interfaces:**
- Produces: `SAVE_VERSION = 3`, `SKIN_ORDER`, `save.floor`, `save.startFloor`, `save.skin`; `T(key, n)`; ключи `floor, massAtLeast, needMass, floorCleared, floorUnlocked, vented`; `fell` = «Шлёп!».

- [ ] **Step 1: Обновить tools/test_save.js под v3**

Заменить `const EMPTY = …` на `const EMPTY = { v: 3, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 }, floor: 0, startFloor: 1, skin: 'none' };` и все ожидаемые объекты в файле привести к v3 (везде `v: 3` и добавить `floor: 0, startFloor: 1, skin: 'none'`): `expected` в блоке 1, `merged` и `snap` в блоке 2, результаты `migrate` в блоке 3, результат `mergeSaves` в блоке 3. В блоке 2 запись `teft_save` перед `loadSave` оставить v2 (`{ v: 2, best: 4, earned: 20, spent: 5, up: {...} }`) — так проверяется миграция v2 → v3 при чтении локали; комментарий блока: `// облако v1 и локаль v2 расходятся → максимум по каждому полю, включая уровни; оба хранилища переписаны в v3`. В конце блока 3 добавить:
```js
    // v3: этажи и скин — миграция, зажим стартового этажа, слияние
    assert.deepStrictEqual(d.migrate({ v: 2, best: 1, earned: 2, spent: 0 }).floor, 0);
    const m3 = d.migrate({ floor: 5, startFloor: 9, skin: 'crown' });
    assert.strictEqual(m3.floor, 5); assert.strictEqual(m3.startFloor, 6, 'startFloor ≤ floor + 1'); assert.strictEqual(m3.skin, 'crown');
    assert.strictEqual(d.migrate({ floor: 2, startFloor: 0, skin: 'hat' }).startFloor, 1); assert.strictEqual(d.migrate({ skin: 'hat' }).skin, 'none', 'неизвестный скин → none');
    const mg = d.mergeSaves({ floor: 2, startFloor: 3, skin: 'chef' }, { floor: 4, startFloor: 1, skin: 'none' });
    assert.deepStrictEqual([mg.floor, mg.startFloor, mg.skin], [4, 3, 'chef']);
    assert.strictEqual(d.mergeSaves({ floor: 1, startFloor: 2, skin: 'none' }, { floor: 0, startFloor: 5, skin: 'glasses' }).startFloor, 2, 'зажим после слияния');
    assert.deepStrictEqual(d.SKIN_ORDER, ['none', 'chef', 'glasses', 'crown', 'bow', 'mustache']);
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_save.js`
Expected: FAIL — `deepStrictEqual` на первом `EMPTY` (`v: 2`, нет `floor`).

- [ ] **Step 3: Переписать src/save.js**

```js
'use strict';
// ---------- сохранения: облако Яндекса + локальный резерв, при загрузке максимум по полям ----------
// v3 — монотонный журнал: earned/spent, уровни апгрейдов и пройденные этажи только растут, скин упорядочен по открытию.
// Поэтому слияние «максимум по полям» корректно: игрок никогда не теряет, а баланс не уходит в минус
// (на каждом устройстве spent ≤ earned, значит max(earned) − max(spent) ≥ 0).
const SAVE_VERSION = 3;
const SAVE_KEY = 'teft_save';
const UP_MAX = { jumps: 2, magnet: 3 };   // максимальные уровни апгрейдов; каталог и цены — в upgrades.js
const SKIN_ORDER = ['none', 'chef', 'glasses', 'crown', 'bow', 'mustache']; // порядок открытия скинов (каталог — в skins.js)
const save = { v: SAVE_VERSION, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 }, floor: 0, startFloor: 1, skin: 'none' };
function coins() { return Math.max(0, save.earned - save.spent); }
const _int = v => Math.max(0, Math.floor(+v || 0));
const _clampStart = (start, floor) => Math.min(Math.max(1, start), floor + 1);
// приводит объект любой версии (или мусор) к текущей схеме; v1 {coins} → earned = coins, spent = 0; v2 → этажи и скин по умолчанию
function migrate(obj) {
  const o = { v: SAVE_VERSION, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 }, floor: 0, startFloor: 1, skin: 'none' };
  if (obj && typeof obj === 'object') {
    o.best = _int(obj.best);
    if (obj.earned !== undefined || obj.spent !== undefined) { o.earned = _int(obj.earned); o.spent = _int(obj.spent); }
    else o.earned = _int(obj.coins);
    const up = obj.up && typeof obj.up === 'object' ? obj.up : {};
    for (const k of Object.keys(UP_MAX)) o.up[k] = Math.min(UP_MAX[k], _int(up[k]));
    o.floor = _int(obj.floor);
    o.startFloor = _clampStart(_int(obj.startFloor) || 1, o.floor);
    o.skin = SKIN_ORDER.includes(obj.skin) ? obj.skin : 'none';
  }
  return o;
}
function mergeSaves(a, b) {
  const o = { v: SAVE_VERSION, best: Math.max(a.best || 0, b.best || 0), earned: Math.max(a.earned || 0, b.earned || 0), spent: Math.max(a.spent || 0, b.spent || 0), up: {},
    floor: Math.max(a.floor || 0, b.floor || 0), startFloor: 1, skin: 'none' };
  for (const k of Object.keys(UP_MAX)) o.up[k] = Math.max((a.up && a.up[k]) || 0, (b.up && b.up[k]) || 0);
  o.startFloor = _clampStart(Math.max(a.startFloor || 1, b.startFloor || 1), o.floor);
  o.skin = SKIN_ORDER[Math.max(Math.max(0, SKIN_ORDER.indexOf(a.skin)), Math.max(0, SKIN_ORDER.indexOf(b.skin)))];
  return o;
}
function snapshot() { return { v: SAVE_VERSION, best: save.best, earned: save.earned, spent: save.spent, up: { jumps: save.up.jumps, magnet: save.up.magnet }, floor: save.floor, startFloor: save.startFloor, skin: save.skin }; }
function sameSave(a, b) { return JSON.stringify(migrate(a)) === JSON.stringify(migrate(b)); }
function _readLocal() {
  const st = YG.storage; if (!st) return null;
  try {
    const raw = st.getItem(SAVE_KEY);
    if (raw) return JSON.parse(raw);
    // миграция с ключей прототипа (v1)
    const b = +st.getItem('teft_best') || 0, c = +st.getItem('teft_coins') || 0;
    if (b || c) { const o = { v: 1, best: b, coins: c }; st.setItem(SAVE_KEY, JSON.stringify(o)); st.removeItem('teft_best'); st.removeItem('teft_coins'); return o; }
  } catch (e) {}
  return null;
}
function _writeLocal(obj) { try { if (YG.storage) YG.storage.setItem(SAVE_KEY, JSON.stringify(obj)); } catch (e) {} }
let lastSent = '';   // последний снимок, отправленный в облако — чтобы не слать повторно один и тот же
async function loadSave() {
  const cloudRaw = await YG.getData(), localRaw = _readLocal();
  const cloud = migrate(cloudRaw), local = migrate(localRaw);
  const merged = mergeSaves(cloud, local);
  Object.assign(save, merged);
  // переписываем хранилище, если оно пустое, старой версии или отличается от слитого
  if (!localRaw || localRaw.v !== SAVE_VERSION || !sameSave(local, merged)) _writeLocal(merged);
  if (!cloudRaw || cloudRaw.v !== SAVE_VERSION || !sameSave(cloud, merged)) YG.setData(merged);
  lastSent = JSON.stringify(merged);
  return save;
}
// вызывать только при смерти, наградах, покупках, пройденном этаже и смене этажа/скина: лимит облака 100 запросов за 5 минут
function persist() {
  const snap = snapshot();
  const json = JSON.stringify(snap);
  _writeLocal(snap);
  if (json !== lastSent) { lastSent = json; YG.setData(snap); }
}
expose({ save, coins, loadSave, persist, migrate, mergeSaves, UP_MAX, SKIN_ORDER });
```

- [ ] **Step 4: `T(key, n)` и строки в src/i18n.js**

В каждой из трёх таблиц заменить значение `fell` и добавить ключи (в конец таблицы):
- ru: `fell: 'Шлёп!'`; `floor: 'Этаж', massAtLeast: 'масса ≥ {n}', needMass: 'Нужна масса {n}', floorCleared: 'Этаж {n} пройден!', floorUnlocked: 'Открыт этаж {n}', vented: 'Вентиляция',`
- en: `fell: 'Splat!'`; `floor: 'Floor', massAtLeast: 'mass ≥ {n}', needMass: 'Need mass {n}', floorCleared: 'Floor {n} cleared!', floorUnlocked: 'Floor {n} unlocked', vented: 'Vent',`
- tr: `fell: 'Pat!'`; `floor: 'Kat', massAtLeast: 'kütle ≥ {n}', needMass: 'Kütle {n} gerek', floorCleared: 'Kat {n} geçildi!', floorUnlocked: 'Kat {n} açıldı', vented: 'Havalandırma',`
Функцию `T` заменить на:
```js
// T(key) — строка; T(key, n) — с подстановкой {n} (число) или {s} (строка)
function T(key, n) {
  for (const tbl of [STR[LANG], STR.en, STR.ru]) {
    const t = tbl[key];
    if (t !== undefined) return n === undefined ? t : t.replace('{n}', String(n)).replace('{s}', String(n));
  }
  return key;
}
```
`tools/test_render.js`: `texts.includes('Упала')` → `texts.includes('Шлёп!')`, `texts.includes('Fell')` → `texts.includes('Splat!')`.

- [ ] **Step 5: Запустить тесты**

Run: `node tools/test_save.js && node tools/test_render.js && node tools/test_core.js && node tools/smoke.js`
Expected: `test_save ok`, `test_render ok`, `test_core ok`, `smoke ok`.

- [ ] **Step 6: Коммит**

```bash
git add src/save.js src/i18n.js tools/test_save.js tools/test_render.js
git commit -m "feat: сохранения v3 (этаж, стартовый этаж, скин), T(key, n), строки этажей, «Шлёп!» вместо «Упала»

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 2: Модуль этажей — разметка, люки, буфет, старт с этажа

**Files:**
- Create: `src/floors.js`, `tools/test_floors.js`
- Modify: `src/world.js` (хук `spawnFilter`), `src/game.js` (`reset(n)`, учёт этажей, `spawnHatches`/`pruneHatches`, `expose`), `index.html`, `package.json`

**Interfaces:**
- Consumes: `save, persist` (save.js), `ball, radiusFor, initBody` (ball.js), `items, FOOD, camY, spawnedTo, popText, burst` (world.js), `W, H, clamp, T, ctx, sfx, tone` (выше).
- Produces (floors.js): `FLOOR_H, FLOOR_PX, BUFFET_M, VENT_TIME, VENT_NEAR`, `floorOf(h)`, `startMass(n)`, `hatchMass(n)`, `hatchBonus(n)`, `hatchY(n)`, `floorBaseY(n)`, `inBuffetZone(h)`, `hatches` (массив), `nextHatchFloor`, `resetHatches(n)`, `spawnHatch(n)`, `spawnHatches()`, `pruneHatches()`, `activeHatch()`, `setStartFloor(n)`; `hatchUpdate(dt)` и `drawHatch(h)` добавляются в Task 3 (здесь — заглушки не нужны, функции появятся в Task 3; `game.js` их в этой задаче не вызывает).
- Produces (game.js): `reset(n = 1)`, `floorReached`, `clearedThisRun`, `newUnlock`, `floorCleared(n)`; DBG `run` получает `floorReached, clearedThisRun, newUnlock`.
- world.js: `let spawnFilter = null;` — `spawn()` пропускает ряд, если `spawnFilter && spawnFilter(h)`.

- [ ] **Step 1: Написать падающий тест tools/test_floors.js (часть 1)**

```js
// node tools/test_floors.js — этажи: разметка, старт с этажа, люк и буфет, пробитие/отскок/вентиляция
const assert = require('assert');
const noop = () => {};
const ctx = new Proxy({}, { get: (t, k) => /Gradient$/.test(k) ? () => ({ addColorStop: noop }) : noop, set: () => true });
(async () => {
  const g = require('./_env')(ctx); const d = g.dbg(); await d.YG.init(); await d.loadSave();
  const ball = d.ball;
  // разметка и числа этажей
  for (const [h, f] of [[0, 1], [149.9, 1], [150, 2], [151, 2], [1500, 11]]) assert.strictEqual(d.floorOf(h), f, 'floorOf ' + h);
  for (const [n, m] of [[1, 1], [2, 3], [5, 6], [11, 12], [20, 12]]) assert.strictEqual(d.startMass(n), m, 'startMass ' + n);
  for (const [n, m] of [[1, 4], [5, 8], [12, 15], [20, 15]]) assert.strictEqual(d.hatchMass(n), m, 'hatchMass ' + n);
  assert.strictEqual(d.hatchBonus(1), 10); assert.strictEqual(d.hatchBonus(5), 30);
  assert.strictEqual(d.hatchY(1), -1500); assert.strictEqual(d.floorBaseY(3), -3000);
  for (const [h, z] of [[129, false], [131, true], [149, true], [150, false], [281, true]]) assert.strictEqual(d.inBuffetZone(h), z, 'buffet zone ' + h);
  // старт с этажа 3: тарелка на полу этажа, стартовая масса, камера, счётчики
  d.reset(3); d.state = 'play';
  assert.strictEqual(ball.y, -3000); assert.strictEqual(ball.mass, 4); assert.strictEqual(d.plates[0].y, -2970);
  assert.strictEqual(d.camY, -3000 - 854 + 120); assert.strictEqual(d.run.maxHeight, 300); assert.strictEqual(d.run.floorReached, 3);
  assert.strictEqual(d.hatches.length, 0); assert.strictEqual(d.nextHatchFloor, 3);
  for (let i = 0; i < 30; i++) d.update(0.016);
  assert.strictEqual(d.state, 'play', 'на тарелке этажа не умирает'); assert.strictEqual(d.run.floorReached, 3);
  // люк и буфет появляются, когда спавн проходит потолок этажа; в зоне буфета обычных предметов нет
  d.reset(1); d.state = 'play'; d.update(0.016);
  assert.strictEqual(d.hatches.length, 0, 'до потолка люка нет');
  ball.y = -1400; for (let i = 0; i < 8; i++) d.update(0.016);
  assert.strictEqual(d.hatches.length, 1); assert.strictEqual(d.hatches[0].floor, 1); assert.strictEqual(d.hatches[0].need, 4); assert.strictEqual(d.hatches[0].y, -1500);
  const meat = d.items.find(it => it.buffet && it.kind === 'meat'), pasta = d.items.find(it => it.buffet && it.kind === 'pasta');
  assert.ok(meat && meat.x === 160 && meat.y === -1380, 'мясо буфета'); assert.ok(pasta && pasta.x === 320 && pasta.y === -1440, 'макароны буфета');
  assert.ok(!d.items.some(it => !it.buffet && d.inBuffetZone(-it.y / 10)), 'под люком обычного спавна нет');
  assert.strictEqual(d.nextHatchFloor, 2);
  // следующий люк — для следующего этажа
  ball.y = -2900; for (let i = 0; i < 20; i++) d.update(0.016);
  assert.strictEqual(d.hatches.length, 2); assert.strictEqual(d.hatches[1].floor, 2); assert.strictEqual(d.hatches[1].need, 5);
  // стартовый этаж зажат в [1, floor + 1] и сохраняется
  d.save.floor = 2; d.setStartFloor(9); assert.strictEqual(d.save.startFloor, 3); d.setStartFloor(0); assert.strictEqual(d.save.startFloor, 1);
  assert.strictEqual(JSON.parse(g.store.get('teft_save')).startFloor, 1, 'стартовый этаж записан');
  // люки ниже экрана удаляются
  d.reset(1); d.state = 'play'; d.hatches.push({ floor: 0, y: 5000, need: 1, broken: true, vented: false, underT: 0, bounceT: 0 }); d.update(0.016);
  assert.strictEqual(d.hatches.length, 0, 'люк за нижним краем удалён');
  console.log('test_floors part1 ok');
  console.log('test_floors ok');
})().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_floors.js`
Expected: FAIL — `TypeError: d.floorOf is not a function`.

- [ ] **Step 3: Написать src/floors.js**

```js
'use strict';
// ---------- этажи башни: разметка по высоте, люк на потолке, буфет перед ним, вентиляция ----------
// Числа черновые (spec 2026-09-23 floors §3–§4) — настраиваются только здесь.
const FLOOR_H = 150;              // метров на этаж
const FLOOR_PX = FLOOR_H * 10;    // то же в px
const BUFFET_M = 20;              // последние метры этажа без обычного спавна — там буфет
const VENT_TIME = 8;              // секунд под люком до вентиляции
const VENT_NEAR = 300;            // px под люком, где копится таймер вентиляции
function floorOf(h) { return Math.floor(h / FLOOR_H) + 1; }
function startMass(n) { return n === 1 ? 1 : Math.min(1 + n, 12); }
function hatchMass(n) { return Math.min(3 + n, 15); }
function hatchBonus(n) { return 5 + 5 * n; }
function floorBaseY(n) { return -FLOOR_PX * (n - 1); }   // мировой y пола этажа n
function hatchY(n) { return -FLOOR_PX * n; }             // мировой y потолка (люка) этажа n
function inBuffetZone(h) { return (h % FLOOR_H) > FLOOR_H - BUFFET_M; }
// world.spawn() не кладёт ряды под люком; запас 3 м с обеих сторон — предметы разбросаны на ±2.5 м от ряда
spawnFilter = h => { const m = h % FLOOR_H; return m > FLOOR_H - BUFFET_M - 3 || m < 3; };
let hatches = [];                 // { floor, y, need, broken, vented, underT, bounceT }
let nextHatchFloor = 1;           // чей потолок спавн ещё не прошёл
function resetHatches(n) { hatches = []; nextHatchFloor = n; }
function buffetFor(n) { // гарантированная еда под люком: мясо + макароны (+3 массы)
  const y = hatchY(n);
  return [{ kind: 'meat', x: 160, y: y + 120 }, { kind: 'pasta', x: 320, y: y + 60 }];
}
function spawnHatch(n) {
  hatches.push({ floor: n, y: hatchY(n), need: hatchMass(n), broken: false, vented: false, underT: 0, bounceT: 0 });
  for (const b of buffetFor(n)) { const def = FOOD[b.kind]; items.push({ kind: b.kind, trash: false, def, x: b.x, y: b.y, r: def.r, vx: 0, seed: Math.random() * 10, dead: false, buffet: true }); }
}
// вызывается из game.updateRun после spawn(): спавн прошёл потолок этажа — кладём люк и буфет
function spawnHatches() { while (spawnedTo <= hatchY(nextHatchFloor)) spawnHatch(nextHatchFloor++); }
function pruneHatches() { hatches = hatches.filter(h => h.y < camY + H + 80); }
function activeHatch() { // ближайший не открытый люк над тефтелей
  let best = null;
  for (const h of hatches) if (!h.broken && !h.vented && h.y < ball.y && (!best || h.y > best.y)) best = h;
  return best;
}
function setStartFloor(n) { save.startFloor = clamp(n, 1, save.floor + 1); persist(); }
expose({ FLOOR_H, VENT_TIME, floorOf, startMass, hatchMass, hatchBonus, hatchY, floorBaseY, inBuffetZone, get hatches() { return hatches; }, get nextHatchFloor() { return nextHatchFloor; }, spawnHatch, spawnHatches, setStartFloor });
```
`clamp` — из core.js.

- [ ] **Step 4: Хук спавна в src/world.js**

Перед `function spawn()` добавить `let spawnFilter = null; // этажи (floors.js) выключают спавн в зоне буфета: spawnFilter(h) → true = ряд пропустить`.
В `spawn()` после строки `const h = -spawnedTo / 10;` добавить `if (spawnFilter && spawnFilter(h)) continue;`.

- [ ] **Step 5: Правки src/game.js**

- Переменные: после `let usedContinue = …` добавить `let floorReached = 1, clearedThisRun = [], newUnlock = 0; // этажи: достигнутый, пройденные за забег, открытый для старта`.
- `reset()` → `reset(n = 1)`:
```js
function reset(n = 1) {
  const baseY = floorBaseY(n);
  ball.x = W / 2; ball.y = baseY; ball.vx = 0; ball.vy = 0; ball.mass = startMass(n); ball.r = radiusFor(ball.mass);
  ball.jumps = maxJumps(); ball.regen = 0; ball.mouth = 0; ball.face = 0; ball.alive = true; initBody();
  camY = baseY - H + 120; items = []; particles = []; texts = []; plates = [{ x: W / 2, y: baseY + 30, w: 220 }];
  spawnedTo = baseY - 160; maxHeight = FLOOR_H * (n - 1); runCoins = 0; bankedCoins = 0; tGame = 0; camShake = 0;
  usedContinue = false; usedDouble = false; invuln = 0; massAtDeath = 0;
  resetHatches(n); floorReached = n; clearedThisRun = []; newUnlock = 0;
}
```
- Перед `function updateRun(dt)` добавить:
```js
// этаж пройден честно: учёт за забег и прогресс сохранения
function floorCleared(n) {
  clearedThisRun.push(n);
  if (n > save.floor) { save.floor = n; newUnlock = n + 1; persist(); }
}
```
- В `updateRun` после строки `maxHeight = Math.max(…)` добавить `floorReached = Math.max(floorReached, floorOf(Math.max(0, -ball.y / 10)));`; после `spawn();` добавить `spawnHatches();`; после `plates = plates.filter(…)` добавить `pruneHatches();`.
- `expose`: `run` → `{ maxHeight, runCoins, usedContinue, usedDouble, invuln, massAtDeath, bankedCoins, floorReached, clearedThisRun, newUnlock }`; добавить `floorCleared` в список.

- [ ] **Step 6: index.html и package.json**

`index.html`: после `<script src="src/world.js"></script>` вставить `<script src="src/floors.js"></script>`. `package.json` `test`: после `node tools/test_upgrades.js` добавить ` && node tools/test_floors.js`.

- [ ] **Step 7: Запустить тесты**

Run: `npm test`
Expected: восемь `… ok` (core, sdk, save, upgrades, floors, game, render, smoke).

- [ ] **Step 8: Коммит**

```bash
git add src/floors.js src/world.js src/game.js index.html package.json tools/test_floors.js
git commit -m "feat: этажи по 150 м — люки и буфет по потолку этажа, старт с любого этажа, учёт пройденных

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 3: Люк в действии — пробитие, отскок, вентиляция, рисование, HUD

**Files:**
- Modify: `src/floors.js` (`hatchUpdate`, `drawHatch`), `src/game.js` (события люка в `updateRun`), `src/render.js` (`drawHUD`, `drawWorld`)
- Test: `tools/test_floors.js` (часть 2)

**Interfaces:**
- Consumes: всё из Task 2; `squash` (ball.js), `burst, popText` (world.js), `sfx, tone` (audio.js), `T` (i18n.js).
- Produces: `hatchUpdate(dt) → null | { type: 'break' | 'bounce' | 'vent', hatch }`, `drawHatch(h)`; `game.js` применяет `camShake`, `runCoins`, `floorCleared`; HUD строка этажа; DBG добавляет `hatchUpdate, drawHatch`.

- [ ] **Step 1: Дописать тесты (часть 2) в tools/test_floors.js** — заменить строку `console.log('test_floors part1 ok');` на:

```js
  const under = h => { ball.y = h.y + 12 + ball.r + 4; ball.vy = -300; ball.vx = 0; }; // подвести к люку снизу и лететь вверх
  // пробитие: масса ≥ need → люк сломан, бонус, этаж засчитан и сохранён, следующий старт открыт
  d.reset(1); d.state = 'play'; d.spawnHatch(1); ball.mass = 5; under(d.hatches[0]); d.YG.gameplayStart();
  const c0 = d.run.runCoins; d.update(0.016);
  assert.ok(d.hatches[0].broken, 'люк пробит'); assert.strictEqual(d.run.runCoins, c0 + 10, 'бонус 5+5·1');
  assert.deepStrictEqual(d.run.clearedThisRun, [1]); assert.strictEqual(d.run.newUnlock, 2); assert.strictEqual(d.save.floor, 1);
  assert.strictEqual(JSON.parse(g.store.get('teft_save')).floor, 1, 'прогресс сохранён'); assert.ok(ball.vy < 0, 'летит дальше вверх');
  // повторное пробитие того же этажа не двигает save.floor и не открывает ничего нового
  d.reset(1); d.state = 'play'; d.spawnHatch(1); ball.mass = 5; under(d.hatches[0]); d.update(0.016);
  assert.strictEqual(d.save.floor, 1); assert.strictEqual(d.run.newUnlock, 0); assert.deepStrictEqual(d.run.clearedThisRun, [1]);
  // отскок: масса < need → люк цел, тефтеля ниже люка, летит вниз, масса не меняется
  d.reset(1); d.state = 'play'; d.spawnHatch(1); ball.mass = 2; under(d.hatches[0]); d.update(0.016);
  assert.ok(!d.hatches[0].broken); assert.strictEqual(ball.mass, 2); assert.ok(ball.vy > 0, 'отскок вниз');
  assert.ok(Math.abs(ball.y - (d.hatches[0].y + 12 + ball.r)) < 1e-6, 'выставлена под люк'); assert.strictEqual(d.run.runCoins, 0);
  // вентиляция: 8 с под люком без пробития → люк открыт без бонуса и без прогресса; после него проход свободен
  d.reset(1); d.state = 'play'; d.spawnHatch(1); ball.mass = 6; d.save.floor = 0; // масса 6: случайный мусор не убьёт, а без vy < 0 пробития нет
  for (let i = 0; i < 520; i++) { ball.y = d.hatches[0].y + 200; ball.vy = 0; d.update(0.016); }
  assert.ok(d.hatches[0].vented, 'вентиляция'); assert.ok(!d.hatches[0].broken); assert.strictEqual(d.run.runCoins, 0); assert.strictEqual(d.save.floor, 0);
  under(d.hatches[0]); d.update(0.016); assert.ok(ball.vy < 0 && ball.y < d.hatches[0].y + 12 + ball.r, 'через вентиляцию проходит без отскока');
  // вне зоны под люком таймер не копится
  d.reset(1); d.state = 'play'; d.spawnHatch(1); ball.mass = 6;
  for (let i = 0; i < 100; i++) { ball.y = d.hatches[0].y + 600; ball.vy = 0; d.update(0.016); }
  assert.strictEqual(d.hatches[0].underT, 0, 'дальше 300 px таймер стоит');
  // рисование не падает
  d.drawHatch(d.hatches[0]); d.hatches[0].broken = true; d.drawHatch(d.hatches[0]); d.hatches[0].broken = false; d.hatches[0].vented = true; d.drawHatch(d.hatches[0]);
  d.drawWorld(); d.drawHUD();
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_floors.js`
Expected: FAIL — `AssertionError: люк пробит` (столкновения ещё нет).

- [ ] **Step 3: `hatchUpdate` и `drawHatch` в src/floors.js** (вставить перед `expose`)

```js
// столкновение с ближайшим люком и таймер вентиляции; вызывается из game.updateRun.
// Возвращает событие для game.js (тряска, бонус, прогресс применяет он): null | { type: 'break'|'bounce'|'vent', hatch }
function hatchUpdate(dt) {
  const h = activeHatch(); if (!h) return null;
  h.bounceT = Math.max(0, h.bounceT - dt);
  if (ball.y <= h.y + VENT_NEAR) {
    h.underT += dt;
    if (h.underT >= VENT_TIME) {
      h.vented = true; tone(300, 500, 0.3, 'triangle', 0.15);
      popText(W / 2, h.y + 60, T('vented'), 'rgba(255,255,255,0.8)', true);
      return { type: 'vent', hatch: h };
    }
  }
  if (ball.vy < 0 && ball.y - ball.r <= h.y + 12) {
    if (ball.mass >= h.need) {
      h.broken = true;
      burst(W / 2, h.y, '#8a7255', 30, 320, 0.8, 5); burst(ball.x, h.y, '#b9542f', 20, 300, 0.7, 5);
      sfx.big();
      popText(W / 2, h.y - 70, T('floorCleared', h.floor), '#fff', true);
      return { type: 'break', hatch: h };
    }
    ball.y = h.y + 12 + ball.r; ball.vy = 200; ball.vx *= 0.7; squash(200);
    if (h.bounceT <= 0) { h.bounceT = 0.5; sfx.hit(); popText(ball.x, ball.y - ball.r - 10, T('needMass', h.need), '#ff7a6b', true); }
    return { type: 'bounce', hatch: h };
  }
  return null;
}
// люк на потолке этажа: полоса во всю ширину с табличкой; сломанный — обломки по краям; вентилируемый — отъехал вправо
function drawHatch(h) {
  const y = h.y - camY; if (y < -40 || y > H + 40) return;
  ctx.save();
  if (h.broken) {
    ctx.fillStyle = '#6d5a48'; ctx.fillRect(0, y - 12, 70, 24); ctx.fillRect(W - 70, y - 12, 70, 24);
    ctx.fillStyle = '#4e3f32'; for (let i = 0; i < 6; i++) { const x = 90 + i * 55; ctx.beginPath(); ctx.moveTo(x, y - 12); ctx.lineTo(x + 18, y + 4); ctx.lineTo(x - 6, y + 8); ctx.closePath(); ctx.fill(); }
  } else {
    const x0 = h.vented ? 140 : 0;
    ctx.fillStyle = '#6d5a48'; ctx.fillRect(x0, y - 12, W - x0, 24);
    ctx.fillStyle = '#4e3f32'; ctx.fillRect(x0, y + 8, W - x0, 4);
    ctx.fillStyle = '#9a8468'; for (let x = x0 + 20; x < W; x += 60) { ctx.beginPath(); ctx.arc(x, y, 3, 0, 7); ctx.fill(); }
    if (!h.vented) {
      ctx.fillStyle = '#2b2118'; ctx.fillRect(W / 2 - 70, y - 22, 140, 44);
      ctx.strokeStyle = '#ffe08a'; ctx.lineWidth = 2; ctx.strokeRect(W / 2 - 70, y - 22, 140, 44);
      ctx.fillStyle = '#ffe08a'; ctx.textAlign = 'center'; ctx.font = '700 15px system-ui, sans-serif'; ctx.fillText(T('massAtLeast', h.need), W / 2, y + 5);
    }
  }
  ctx.restore();
}
```
и в `expose` добавить `hatchUpdate, drawHatch`.

- [ ] **Step 4: События люка в src/game.js**

В `updateRun` сразу после цикла по `plates` (перед `const target = …`) добавить:
```js
  const hv = hatchUpdate(dt); // люк: пробитие даёт тряску, бонус и прогресс; отскок — лёгкую тряску
  if (hv && hv.type === 'break') {
    camShake = 12; const bonus = hatchBonus(hv.hatch.floor); runCoins += bonus;
    popText(ball.x, hv.hatch.y - 30, '+' + bonus, '#ffe08a', true); floorCleared(hv.hatch.floor);
  } else if (hv && hv.type === 'bounce') camShake = 4;
```

- [ ] **Step 5: Рисование и HUD в src/render.js**

В `drawWorld()` перед `for (const it of items) drawItem(it);` добавить `for (const h of hatches) drawHatch(h);`.
В `drawHUD()` после строки `ctx.fillText(maxHeight + ' ' + T('m'), 16, 44);` добавить:
```js
  ctx.fillStyle = 'rgba(255,255,255,0.7)'; ctx.font = '600 15px system-ui, sans-serif';
  ctx.fillText(T('floor') + ' ' + floorOf(Math.max(0, -ball.y / 10)), 16, 66);
  ctx.fillStyle = '#fff';
```

- [ ] **Step 6: Запустить тесты**

Run: `npm test`
Expected: восемь `… ok`.

- [ ] **Step 7: Коммит**

```bash
git add src/floors.js src/game.js src/render.js tools/test_floors.js
git commit -m "feat: люк — пробитие массой с бонусом и прогрессом, отскок, вентиляция через 8 с; рисование люка и строка этажа в HUD

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 4: Селектор этажа на титуле, строки результатов, старт с выбранного этажа

**Files:**
- Modify: `src/screens.js` (`titleScreen`, `resultsScreen`), `src/main.js` (`boot`, `restart`, `onTap`)
- Test: `tools/test_render.js`, `tools/smoke.js`

**Interfaces:**
- Consumes: `save.floor`, `save.startFloor`, `setStartFloor(n)`, `startMass(n)`, `floorBaseY(n)` (floors.js), `run.floorReached/newUnlock` (game.js).
- Produces: кнопки `floor-`, `floor+` (титул, только при `save.floor ≥ 1`), кнопка `shop` на `H·0.64`, `tapToJump` на `H·0.75`; строки результатов `T('floor') + ' ' + floorReached`, `T('floorUnlocked', newUnlock)`; `boot()`/`restart()` стартуют с `save.startFloor`.

- [ ] **Step 1: Дописать тесты**

`tools/test_render.js` — перед `console.log('test_render ok');`:
```js
  // этажи: селектор на титуле только при пройденных этажах; строки результатов
  d.save.floor = 0; d.save.startFloor = 1; d.reset(); d.state = 'title'; d.titleScreen();
  assert.ok(!d.buttons.some(b => b.id === 'floor-' || b.id === 'floor+'), 'без пройденных этажей селектора нет');
  d.save.floor = 2; d.save.startFloor = 3; texts.length = 0; d.titleScreen();
  const fm = d.buttons.find(b => b.id === 'floor-'), fp = d.buttons.find(b => b.id === 'floor+');
  assert.ok(fm && fp, 'кнопки селектора'); assert.ok(texts.includes('Этаж 3'), 'текущий стартовый этаж');
  assert.ok(fm.y === fp.y && fm.y < d.buttons.find(b => b.id === 'shop').y, 'селектор выше кнопки Магазин');
  d.save.floor = 0; d.save.startFloor = 1; d.reset(1); d.state = 'play'; d.spawnHatch(1); d.ball.mass = 5; d.ball.y = d.hatches[0].y + 12 + d.ball.r + 4; d.ball.vy = -300; d.update(0.016);
  assert.strictEqual(d.run.newUnlock, 2); d.die(); texts.length = 0; d.resultsScreen(true);
  assert.ok(texts.includes('Этаж 1') && texts.includes('Открыт этаж 2'), 'строки этажа на результатах: достигнут 1-й, открыт 2-й');
  texts.length = 0; d.setLang('en'); d.resultsScreen(true); assert.ok(texts.includes('Floor 2 unlocked')); d.setLang('ru');
  texts.length = 0; d.state = 'play'; d.drawHUD(); assert.ok(texts.some(t => t.startsWith('Этаж ')), 'HUD показывает этаж');
  d.save.floor = 0; d.save.startFloor = 1;
```
`tools/smoke.js` — в финальный IIFE после блока «магазин с титула»:
```js
  // этажи: пробитие люка открывает этаж 2; селектор на титуле; старт со 2 этажа со стартовой массой
  { const g = require('./_env')(ctx); const d = g.dbg(); await g.boot(); g.tap(240);
    d.ball.mass = 6; d.ball.y = -1400; steps(g, 8); assert.ok(d.hatches.length >= 1, 'люк первого этажа создан');
    d.ball.y = d.hatches[0].y + 12 + d.ball.r + 4; d.ball.vy = -300; g.step();
    assert.ok(d.hatches[0].broken, 'люк пробит'); assert.strictEqual(d.save.floor, 1); assert.strictEqual(d.run.newUnlock, 2);
    d.die(); steps(g, 60); assert.strictEqual(d.state, 'dead');
    assert.strictEqual(JSON.parse(g.store.get('teft_save')).floor, 1, 'этаж сохранён'); }
  { const g = require('./_env')(ctx);
    g.store.set('teft_save', JSON.stringify({ v: 3, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 }, floor: 2, startFloor: 2, skin: 'none' }));
    const d = g.dbg(); await g.boot(); g.step();
    const fp = btn(d, 'floor+'); assert.ok(fp, 'кнопка floor+ при пройденных этажах');
    g.tap(fp.x, fp.y); g.step(); assert.strictEqual(d.save.startFloor, 3);
    g.tap(fp.x, fp.y); g.step(); assert.strictEqual(d.save.startFloor, 3, 'выше floor+1 нельзя');
    const fm = btn(d, 'floor-'); g.tap(fm.x, fm.y); g.step(); assert.strictEqual(d.save.startFloor, 2);
    assert.strictEqual(d.state, 'title', 'селектор не стартует игру');
    g.tap(240, 100); assert.strictEqual(d.state, 'play'); assert.strictEqual(d.ball.mass, 3, 'стартовая масса этажа 2'); assert.strictEqual(d.run.maxHeight, 150);
    assert.strictEqual(d.YG.log.at(-1), 'start'); }
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_render.js`
Expected: FAIL — `AssertionError: кнопки селектора`.

- [ ] **Step 3: Правки src/screens.js**

`titleScreen()` — заменить строку с кнопкой `shop` и две строки `tapToJump` на:
```js
  if (save.floor >= 1) { // селектор стартового этажа: от первого до последнего пройденного + 1
    const ys = H * 0.57;
    ctx.fillStyle = '#fff'; ctx.font = '800 22px system-ui, sans-serif'; ctx.textAlign = 'center';
    ctx.fillText(T('floor') + ' ' + save.startFloor, W / 2, ys + 8);
    button('floor-', W / 2 - 100, ys, 44, 44, '◀', '', false, save.startFloor > 1 ? 'neutral' : 'dim');
    button('floor+', W / 2 + 100, ys, 44, 44, '▶', '', false, save.startFloor < save.floor + 1 ? 'neutral' : 'dim');
  }
  const sb = button('shop', W / 2, H * 0.64, 220, 52, T('shop'), '', false, 'neutral'); if (canBuyAny()) badge(sb);
  const p = 0.5 + Math.sin(tGame * 5) * 0.5;
  ctx.fillStyle = `rgba(255,224,138,${0.6 + p * 0.4})`; ctx.font = '800 26px system-ui, sans-serif';
  ctx.fillText(T('tapToJump'), W / 2, H * 0.75);
```
`resultsScreen()` — массив `lines` дополнить: после `T('total') + ': ' + coins()` добавить элементы `T('floor') + ' ' + floorReached` и, если `newUnlock`, `T('floorUnlocked', newUnlock)`:
```js
  const lines = [maxHeight + ' ' + T('m') + '  ·  ' + T('best') + ' ' + save.best + ' ' + T('m'), T('runCoins') + ': ' + runCoins + (usedDouble ? '  ×2' : ''), T('total') + ': ' + coins(), T('floor') + ' ' + floorReached];
  if (newUnlock) lines.push(T('floorUnlocked', newUnlock));
```

- [ ] **Step 4: Правки src/main.js**

`boot()`: `reset(); state = 'title';` → `reset(save.startFloor); state = 'title';`. `restart()`: `reset(); state = 'play';` → `reset(save.startFloor); state = 'play';`.
В `onTap`, ветка `title`:
```js
  if (state === 'title') {
    const b = hitButton(x, y);
    if (b && b.id === 'shop') { openShop('title'); return; }
    if (b && (b.id === 'floor-' || b.id === 'floor+')) { setStartFloor(save.startFloor + (b.id === 'floor+' ? 1 : -1)); reset(save.startFloor); return; }
    state = 'play'; YG.gameplayStart(); jump(x < ball.x ? -1 : 1); return;
  }
```
(`reset(save.startFloor)` после смены — чтобы тефтеля на титуле уже стояла на выбранном этаже с его массой.)

- [ ] **Step 5: Запустить тесты**

Run: `npm test`
Expected: восемь `… ok`, `smoke ok`.

- [ ] **Step 6: Коммит**

```bash
git add src/screens.js src/main.js tools/test_render.js tools/smoke.js
git commit -m "feat: выбор стартового этажа на титуле, этаж и открытие на результатах, старт с выбранного этажа

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 5: Скриншоты, README, журнал

**Files:**
- Modify: `tools/shot.js`, `README.md`, `docs/DEVLOG.md`

**Interfaces:**
- Consumes: DBG `spawnHatch, hatches, ball, reset, save`; `g.store` до `boot()` для сохранения с пройденными этажами.
- Produces: `shots/hatch.png`, `shots/title_floors.png`, обновлённый `shots/results.png`; README и DEVLOG под этап 3a.

- [ ] **Step 1: Дополнить tools/shot.js**

После `const shop = …` добавить:
```js
// подход к люку первого этажа: буфет под ним, табличка «масса ≥ 4», тефтеля массы 3 чуть ниже
const hatch = async (g, d) => { g.tap(240); d.ball.mass = 3; d.ball.y = -1400; for (let i = 0; i < 8; i++) g.step(); d.ball.y = d.hatches[0].y + 260; d.ball.x = 240; d.ball.vx = 0; d.ball.vy = 0; for (let i = 0; i < 6; i++) { d.ball.vy = 0; g.step(); } };
const withFloors = { store: { teft_save: JSON.stringify({ v: 3, best: 420, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 }, floor: 2, startFloor: 3, skin: 'none' }) } };
```
В `shoot()` перед `await g.boot();` добавить поддержку заранее заданного сохранения: `if (opts.store) for (const k of Object.keys(opts.store)) g.store.set(k, opts.store[k]);`.
В список съёмок добавить:
```js
  await shoot('hatch', 480, 854, hatch);
  await shoot('title_floors', 480, 854, async g => { g.step(); }, withFloors);
```

- [ ] **Step 2: Снять и посмотреть**

Run: `node tools/shot.js`
Expected: двенадцать строк `shots/*.png`. Посмотреть глазами:
- `hatch.png`: горизонтальная полоса люка с заклёпками и табличкой «масса ≥ 4», под ней мясо и макароны буфета, ниже тефтеля; HUD со строкой «Этаж 1» под метрами.
- `title_floors.png`: «◀ Этаж 3 ▶» над кнопкой «Магазин», тефтеля на тарелке не перекрыта, «Тапни, чтобы прыгнуть» между кнопкой и тефтелей.
- `results.png`: заголовок «Шлёп!», строка «Этаж 1» после «Всего», четыре кнопки помещаются.
Расхождения — описать в отчёте, не править рендер молча.

- [ ] **Step 3: README**

- «Правила»: после пункта про «Продолжить/×2» добавить:
  `- **Этажи** (\`src/floors.js\`): башня размечена по 150 м. На потолке этажа люк — пробивается массой ≥ 3+N (до 15), под ним буфет (мясо + макароны). Пробитие: бонус 5+5·N монет, этаж открыт для старта. Без массы через 8 с — вентиляция: проход без награды и без открытия. Старт с любого открытого этажа со стартовой массой 1+N (до 12); выбор стрелками на титуле.`
- «Устройство кода», таблица: строка `src/floors.js` («этажи: разметка, люки, буфет, вентиляция, стартовый этаж») после `src/world.js`; строка `src/save.js` → `save {v:3, …, floor, startFloor, skin}`; строка `src/i18n.js` → упомянуть `T(key, n)`.
- Порядок скриптов в тексте (если упоминается) — с `floors.js` после `world.js`.
- «Как проверять»: `npm test` — «семь тестовых скриптов и smoke».
- «План этапов»: `3. Этажи башни — 3a ✅ (\`docs/specs/2026-09-23-floors-skins-design.md\`), 3b скины и реплики Тефы — следующий.`
- Добавить раздел `## Тексты` с одним правилом: `Персонаж — Тефа, без рода: в русских строках нет гендерных форм («Шлёп!», а не «Упала»).`

- [ ] **Step 4: DEVLOG**

Заменить блок «Текущий статус»:
```markdown
## Текущий статус

- Этап 3a «Этажи башни» реализован: этажи по 150 м, люк по массе с буфетом и вентиляцией, старт с открытого этажа,
  сохранения v3, «Шлёп!» вместо «Упала». `npm test` (8 скриптов) / `npm run shot` / `npm run build` зелёные.
- Следующий — 3b: скины с лёгким эффектом и реплики Тефы (`docs/specs/2026-09-23-floors-skins-design.md` §5–§6).
- SDK живьём не проверялся (нужен черновик в консоли владельца). Экономика и графика черновые.
```
Вставить под вводным абзацем запись:
```markdown
## 2026-09-23 — Сессия 4: этап 3a, этажи и люк

**Решено**
- Башня бесконечная, этажи по 150 м открываются; старт с любого открытого со стартовой массой 1+N. Причина: видимый прогресс без повтора лёгких этажей.
- Испытание — люк по массе (3+N) с буфетом; без массы вентиляция через 8 с без награды и без открытия этажа. Причина: собрано из массы, еды и множителя, без новых сущностей с поведением.
- Косметика за 1-й и каждый 5-й этаж (владелец: за каждый — обесценивает), скины с лёгким эффектом — этап 3b.
- Персонаж — Тефа, без рода: «Упала» → «Шлёп!», реплики без гендерных форм.
- Архитектура: `floors.js` после `world.js`; `world.spawn()` получает хук `spawnFilter`; `hatchUpdate` возвращает событие, а тряску, бонус и прогресс применяет `game.js`. Причина: зависимости только вниз.
- Сохранения v3: `floor`, `startFloor` (зажат в [1, floor+1]), `skin` по порядку открытия — слияние по максимуму остаётся корректным.

**Отвергнуто**
- Бесконечный забег с этажами-вехами и старт всегда с первого; этажи как отдельные уровни с задачами.
- Босс с таймингом и полоса мусора как испытание.
- Этаж как сгенерированный сегмент (переписывание спавна).

**Сделано**
- Задачи 1–5 плана `docs/plans/2026-09-23-floors.md`.

**Дальше**
- План 3b (скины, реплики); владелец — черновик в консоли Яндекса.

**Открытые вопросы**
- Числа `hatchMass`/`startMass`/`hatchBonus`/`VENT_TIME` черновые. Вентиляция не открывает этаж — пересмотреть, если игроки застревают.
```

- [ ] **Step 5: Финальная проверка и коммит**

Run: `npm test && npm run build && git status --short`
Expected: восемь `… ok`, `smoke ok (dist)`, размеры; в статусе только shot.js, README, DEVLOG.

```bash
git add tools/shot.js README.md docs/DEVLOG.md
git commit -m "docs: скриншоты люка и селектора этажей, README и журнал под этап 3a

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
