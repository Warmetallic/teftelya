# Этап 2. Мета-слой: магазин апгрейдов, щадящий старт, злой множитель, сохранения v2, турецкий — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать игроку причину возвращаться: монеты покупают заряды (до 6) и магнит (3 уровня) в отдельном экране магазина; старт щадящий (мусор по одному виду с 25/60/100 м), множитель злее (×1.5/2/3/4), сохранения v2 не теряют покупки, добавлен турецкий язык.

**Architecture:** Новый модуль `src/upgrades.js` (каталог, цены, эффекты, покупка) встаёт между `save.js` и `ball.js`; `save.js` переходит на монотонный журнал `earned/spent` с `coins()`; `game.js`/`world.js`/`render.js` читают `maxJumps()`, `magnetRadius()`, гейты спавна; `screens.js` получает экран магазина и кнопки входа; `main.js` — состояние `shop`. Все модули — обычные скрипты в одном глобальном лексическом окружении, порядок задаёт `index.html`, хуки тестов через `expose()`.

**Tech Stack:** Vanilla JS, Canvas 2D, Node 22 для тестов (`tools/_env.js`, `node tools/test_*.js`, `tools/smoke.js`), node-canvas для скриншотов, системный `zip` для сборки.

**Spec:** `docs/specs/2026-09-23-meta-shop-design.md` — план аргументирует от неё; исполнитель читает оба документа. Этап 1: `docs/specs/2026-09-10-foundation-sdk-design.md`.

## Global Constraints

- Модули — обычные скрипты; верхнеуровневые `const/let/function` видны между файлами; зависимости только «вниз» по списку `index.html`; каждый модуль заканчивается `expose({...})` (core.js), не `Object.assign(DBG, …)`.
- Схема сохранения v2: `{ v: 2, best, earned, spent, up: { jumps: 0..2, magnet: 0..3 } }`, `coins() = max(0, earned - spent)`; слияние — максимум по каждому полю, включая `up.*`; миграция v1: `earned = coins, spent = 0`.
- Каталог: `jumps` уровни `[{price: 80, value: 5}, {price: 300, value: 6}]` (`value` = максимум зарядов, база `JUMPS_BASE = 4`); `magnet` уровни `[{price: 60, value: 60}, {price: 180, value: 90}, {price: 400, value: 120}]` (`value` = радиус в px от края тефтели). Экономика черновая, цены только в `UPGRADES`.
- Гейты спавна `TRASH_FROM = { hair: 25, dirt: 60, fly: 100 }` (метры); `trashP` не меняется.
- Множитель `COIN_MULT = [[14, 4], [10, 3], [7, 2], [4, 1.5]]`, иначе 1. Флаг `big` всплывающего текста — по множителю до прироста массы.
- Магнит: `MAGNET_SPEED = 300` px/с, только `!it.trash`, условие `dist < ball.r + magnetRadius()`.
- Языки: `ru`, `tr`, иначе `en`; `T()` ищет в текущем, затем `en`, затем `ru`, затем возвращает ключ.
- Магазин: состояние `shop`, возврат в `title` или `dead`; из магазина `GameplayAPI` не вызывается; кнопки `shop` (титул `H*0.62`, результаты — 4-я после `again`), `buy:<id>`, `back`; бейдж-точка при `canBuyAny()`.
- Все команды проверки: `npm test` (добавляется `test_upgrades.js`), `npm run shot`, `npm run build`; вывод в отчётах вставлять полностью или с явной пометкой `…(N строк опущено)`.
- Коммиты на русском с трейлером `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; никаких `TBD`/`TODO`.

## Карта файлов

| Файл | Ответственность в этапе 2 |
|---|---|
| `src/save.js` | схема v2, `coins()`, `UP_MAX`, миграция, слияние, запись |
| `src/upgrades.js` (новый) | `UPGRADES`, `JUMPS_BASE`, `upLevel/upMaxLevel/upValue/upPrice`, `maxJumps`, `magnetRadius`, `canBuy`, `canBuyAny`, `buy` |
| `src/i18n.js` | строки магазина, турецкий язык, правило выбора языка |
| `src/audio.js` | `sfx.buy` |
| `src/world.js` | `TRASH_FROM`, `trashPoolFor(h)`, гейты в `spawn()` |
| `src/game.js` | `COIN_MULT`/`coinMult()`, `maxJumps()` вместо `MAXJ`, магнит, `earned` вместо `coins`, состояние `shop` в комментарии |
| `src/render.js` | HUD: число иконок зарядов по `maxJumps()` |
| `src/screens.js` | `button(..., style)`, `badge`, кнопка `shop` на титуле и результатах, `shopScreen()`, `flashCard()` |
| `src/main.js` | `shopReturn`, `openShop/closeShop`, ввод в магазине, `Escape`, `draw()` |
| `index.html`, `package.json` | `src/upgrades.js` в списке скриптов; `test_upgrades.js` в `npm test` |
| `tools/test_save.js`, `tools/test_upgrades.js` (новый), `tools/test_core.js`, `tools/test_game.js`, `tools/test_render.js`, `tools/smoke.js`, `tools/shot.js` | тесты и скриншоты |
| `README.md`, `docs/DEVLOG.md` | правила, магазин, языки, план этапов, запись сессии |

---

### Task 1: Сохранения v2 — монотонный журнал `earned/spent`

**Files:**
- Modify: `src/save.js` (весь файл), `src/game.js:83,100` (две строки `save.coins += …`), `src/screens.js:42` (`save.coins` → `coins()`), `tools/test_game.js`, `tools/smoke.js` (замена `d.save.coins` → `d.coins()`)
- Test: `tools/test_save.js` (переписать)

**Interfaces:**
- Consumes: `YG.getData/setData/storage` (sdk.js), `expose` (core.js).
- Produces: `SAVE_VERSION = 2`, `UP_MAX = { jumps: 2, magnet: 3 }`, объект `save {v, best, earned, spent, up}`, `coins() → number`, `migrate(obj)`, `mergeSaves(a, b)`, `loadSave()`, `persist()`; DBG: `save, coins, loadSave, persist, migrate, mergeSaves, UP_MAX`.

- [ ] **Step 1: Переписать тест tools/test_save.js**

```js
// node tools/test_save.js — сохранения v2: загрузка/слияние/миграция/запись; earned/spent только растут
const assert = require('assert');
const { fakeYaGames } = require('./test_sdk');
const noop = () => {};
const ctx = new Proxy({}, { get: (t, k) => /Gradient$/.test(k) ? () => ({ addColorStop: noop }) : noop, set: () => true });
const EMPTY = { v: 2, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 } };

(async () => {
  { // пусто везде → нули; миграция старых ключей прототипа; локаль v1 перезаписывается v2
    const g = require('./_env')(ctx); const d = g.dbg(); await d.YG.init();
    const s = await d.loadSave(); assert.deepStrictEqual(s, EMPTY); assert.strictEqual(d.coins(), 0);
    g.store.set('teft_best', '42'); g.store.set('teft_coins', '7'); g.store.delete('teft_save');
    const s2 = await d.loadSave(); assert.strictEqual(s2.best, 42); assert.strictEqual(s2.earned, 7); assert.strictEqual(d.coins(), 7);
    assert.ok(!g.store.has('teft_best'), 'старые ключи удалены');
    const expected = { v: 2, best: 42, earned: 7, spent: 0, up: { jumps: 0, magnet: 0 } };
    assert.deepStrictEqual(JSON.parse(g.store.get('teft_save')), expected, 'резерв переписан в v2');
    assert.deepStrictEqual(JSON.parse(g.store.get('teft_cloud_mock')), expected, 'облако-заглушка записано');
  }
  { // облако v1 и локаль v2 расходятся → максимум по каждому полю, включая уровни; оба хранилища выровнены
    const log = []; const g = require('./_env')(ctx, { YaGames: fakeYaGames(log, { cloud: { v: 1, best: 10, coins: 3 } }) });
    const d = g.dbg(); await d.YG.init();
    g.store.set('teft_save', JSON.stringify({ v: 2, best: 4, earned: 20, spent: 5, up: { jumps: 1, magnet: 0 } }));
    const s = await d.loadSave(); await g.flush();
    const merged = { v: 2, best: 10, earned: 20, spent: 5, up: { jumps: 1, magnet: 0 } };
    assert.deepStrictEqual(s, merged); assert.strictEqual(d.coins(), 15);
    assert.deepStrictEqual(JSON.parse(g.store.get('teft_save')), merged);
    assert.ok(log.includes('setData'), 'облако дописано (было v1)');
    assert.deepStrictEqual(await d.YG.getData(), merged);
    // persist пишет оба хранилища, одинаковый снимок в облако не шлёт
    d.save.best = 55; d.save.earned = 30; d.save.up.magnet = 2; d.persist(); await g.flush();
    const snap = { v: 2, best: 55, earned: 30, spent: 5, up: { jumps: 1, magnet: 2 } };
    assert.deepStrictEqual(JSON.parse(g.store.get('teft_save')), snap);
    assert.deepStrictEqual(await d.YG.getData(), snap);
    const n = log.filter(x => x === 'setData').length;
    d.persist(); await g.flush();
    assert.strictEqual(log.filter(x => x === 'setData').length, n, 'одинаковый снимок в облако не шлётся');
    d.save.spent = 6; d.persist(); await g.flush();
    assert.strictEqual(log.filter(x => x === 'setData').length, n + 1);
  }
  { // мусор в хранилищах не роняет загрузку; баланс не уходит в минус; уровни зажимаются
    const g = require('./_env')(ctx); const d = g.dbg(); await d.YG.init();
    g.store.set('teft_save', '{oops'); g.store.set('teft_cloud_mock', '"str"');
    const s = await d.loadSave(); assert.deepStrictEqual(s, EMPTY);
    assert.deepStrictEqual(d.migrate({ best: '12', coins: -5, junk: 1 }), { v: 2, best: 12, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 } });
    assert.deepStrictEqual(d.migrate({ earned: 10, spent: 30, up: { jumps: 9, magnet: -1 } }), { v: 2, best: 0, earned: 10, spent: 30, up: { jumps: 2, magnet: 0 } });
    g.store.set('teft_save', JSON.stringify({ v: 2, earned: 10, spent: 30 })); g.store.delete('teft_cloud_mock');
    await d.loadSave(); assert.strictEqual(d.coins(), 0, 'spent > earned → баланс 0, не минус');
    assert.deepStrictEqual(d.mergeSaves({ best: 1, earned: 9, spent: 2, up: { jumps: 0, magnet: 1 } }, { best: 5, earned: 2, spent: 4, up: { jumps: 1, magnet: 0 } }),
      { v: 2, best: 5, earned: 9, spent: 4, up: { jumps: 1, magnet: 1 } });
    assert.deepStrictEqual(d.UP_MAX, { jumps: 2, magnet: 3 });
  }
  console.log('test_save ok');
})().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_save.js`
Expected: FAIL — `AssertionError` на первом `deepStrictEqual(s, EMPTY)` (пришёл `{ v: 1, best: 0, coins: 0 }`).

- [ ] **Step 3: Переписать src/save.js**

```js
'use strict';
// ---------- сохранения: облако Яндекса + локальный резерв, при загрузке максимум по полям ----------
// v2 — монотонный журнал: earned/spent только растут, баланс = earned − spent; уровни апгрейдов тоже только растут.
// Поэтому слияние «максимум по полям» корректно: игрок никогда не теряет, а баланс не уходит в минус
// (на каждом устройстве spent ≤ earned, значит max(earned) − max(spent) ≥ 0).
const SAVE_VERSION = 2;
const SAVE_KEY = 'teft_save';
const UP_MAX = { jumps: 2, magnet: 3 };   // максимальные уровни апгрейдов; каталог и цены — в upgrades.js
const save = { v: SAVE_VERSION, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 } };
function coins() { return Math.max(0, save.earned - save.spent); }
const _int = v => Math.max(0, Math.floor(+v || 0));
// приводит объект любой версии (или мусор) к текущей схеме; v1 {coins} → earned = coins, spent = 0
function migrate(obj) {
  const o = { v: SAVE_VERSION, best: 0, earned: 0, spent: 0, up: { jumps: 0, magnet: 0 } };
  if (obj && typeof obj === 'object') {
    o.best = _int(obj.best);
    if (obj.earned !== undefined || obj.spent !== undefined) { o.earned = _int(obj.earned); o.spent = _int(obj.spent); }
    else o.earned = _int(obj.coins);
    const up = obj.up && typeof obj.up === 'object' ? obj.up : {};
    for (const k of Object.keys(UP_MAX)) o.up[k] = Math.min(UP_MAX[k], _int(up[k]));
  }
  return o;
}
function mergeSaves(a, b) {
  const o = { v: SAVE_VERSION, best: Math.max(a.best || 0, b.best || 0), earned: Math.max(a.earned || 0, b.earned || 0), spent: Math.max(a.spent || 0, b.spent || 0), up: {} };
  for (const k of Object.keys(UP_MAX)) o.up[k] = Math.max((a.up && a.up[k]) || 0, (b.up && b.up[k]) || 0);
  return o;
}
function snapshot() { return { v: SAVE_VERSION, best: save.best, earned: save.earned, spent: save.spent, up: { jumps: save.up.jumps, magnet: save.up.magnet } }; }
function sameSave(a, b) { return JSON.stringify(migrate(a)) === JSON.stringify(migrate(b)); }
function _readLocal() {
  const st = YG.storage; if (!st) return null;
  try {
    const raw = st.getItem(SAVE_KEY);
    if (raw) return JSON.parse(raw);
    // миграция с ключей прототипа (v1)
    const b = +st.getItem('teft_best') || 0, c = +st.getItem('teft_coins') || 0;
    if (b || c) { const o = { v: 1, best: b, coins: c }; st.setItem(SAVE_KEY, JSON.stringify(o)); st.removeItem('teft_best'); st.removeItem('teft_coins'); return o; }
  } catch (e) {}
  return null;
}
function _writeLocal(obj) { try { if (YG.storage) YG.storage.setItem(SAVE_KEY, JSON.stringify(obj)); } catch (e) {} }
let lastSent = '';   // последний снимок, отправленный в облако — чтобы не слать повторно один и тот же
async function loadSave() {
  const cloudRaw = await YG.getData(), localRaw = _readLocal();
  const cloud = migrate(cloudRaw), local = migrate(localRaw);
  const merged = mergeSaves(cloud, local);
  Object.assign(save, merged);
  // переписываем хранилище, если оно пустое, старой версии или отличается от слитого
  if (!localRaw || localRaw.v !== SAVE_VERSION || !sameSave(local, merged)) _writeLocal(merged);
  if (!cloudRaw || cloudRaw.v !== SAVE_VERSION || !sameSave(cloud, merged)) YG.setData(merged);
  lastSent = JSON.stringify(merged);
  return save;
}
// вызывать только при смерти, наградах и покупках: лимит облака 100 запросов за 5 минут
function persist() {
  const snap = snapshot();
  const json = JSON.stringify(snap);
  _writeLocal(snap);
  if (json !== lastSent) { lastSent = json; YG.setData(snap); }
}
expose({ save, coins, loadSave, persist, migrate, mergeSaves, UP_MAX });
```

- [ ] **Step 4: Перевести потребителей `save.coins` на `earned`/`coins()`**

`src/game.js`, строка `save.coins += runCoins - bankedCoins; bankedCoins = runCoins; …` → `save.earned += runCoins - bankedCoins; bankedCoins = runCoins; …`; строка `doubleCoins` → `function doubleCoins() { save.earned += runCoins; runCoins *= 2; bankedCoins = runCoins; usedDouble = true; persist(); }`; комментарий у `bankedCoins` → `// сколько из runCoins уже зачислено в save.earned — die() банкует по шагам, а не всё сразу`.
`src/screens.js`, строка `lines` в `resultsScreen`: `T('total') + ': ' + save.coins` → `T('total') + ': ' + coins()`.
Тесты: `sed -i 's/d\.save\.coins/d.coins()/g' tools/test_game.js tools/smoke.js` (в них баланс только читается; запись через `die/doubleCoins`).

- [ ] **Step 5: Запустить тесты, убедиться, что проходят**

Run: `node tools/test_save.js && node tools/test_game.js && node tools/test_render.js && node tools/smoke.js`
Expected: `test_save ok`, `test_game ok`, `test_render ok`, `smoke ok`.

- [ ] **Step 6: Коммит**

```bash
git add src/save.js src/game.js src/screens.js tools/test_save.js tools/test_game.js tools/smoke.js
git commit -m "feat: сохранения v2 — монотонный журнал earned/spent, уровни апгрейдов, слияние по максимуму

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 2: Модуль апгрейдов, строки магазина, турецкий язык, звук покупки

**Files:**
- Create: `src/upgrades.js`, `tools/test_upgrades.js`
- Modify: `src/i18n.js` (весь файл), `src/audio.js` (`sfx`), `index.html` (список скриптов), `package.json` (`test`), `tools/test_core.js` (две строки с `'tr'`)

**Interfaces:**
- Consumes: `save, coins, persist, UP_MAX` (save.js), `sfx` (audio.js), `expose` (core.js).
- Produces: `JUMPS_BASE = 4`, `UPGRADES`, `upLevel(id) → 0..max`, `upMaxLevel(id)`, `upValue(id, level)`, `upPrice(id) → number|null`, `maxJumps() → 4..6`, `magnetRadius() → 0|60|90|120`, `canBuy(id)`, `canBuyAny()`, `buy(id) → boolean`; `sfx.buy()`; `setLang('ru'|'tr'|…)`, `T(key)` с цепочкой `LANG → en → ru → key`; строки `shop, back, buy, max, up.jumps, up.magnet, up.jumps.effect, up.magnet.effect, none` на трёх языках.

- [ ] **Step 1: Написать падающий тест tools/test_upgrades.js**

```js
// node tools/test_upgrades.js — каталог апгрейдов: цены, уровни, покупка, эффекты; языки магазина
const assert = require('assert');
const noop = () => {};
const ctx = new Proxy({}, { get: (t, k) => /Gradient$/.test(k) ? () => ({ addColorStop: noop }) : noop, set: () => true });
(async () => {
  const g = require('./_env')(ctx); const d = g.dbg(); await d.YG.init(); await d.loadSave();
  assert.strictEqual(d.JUMPS_BASE, 4);
  assert.strictEqual(d.maxJumps(), 4); assert.strictEqual(d.magnetRadius(), 0);
  assert.strictEqual(d.upPrice('jumps'), 80); assert.strictEqual(d.upPrice('magnet'), 60);
  assert.strictEqual(d.upMaxLevel('jumps'), 2); assert.strictEqual(d.upMaxLevel('magnet'), 3);
  assert.ok(!d.canBuy('jumps') && !d.canBuyAny(), 'без монет купить нельзя');
  assert.strictEqual(d.buy('jumps'), false); assert.strictEqual(d.upLevel('jumps'), 0); assert.strictEqual(d.save.spent, 0);
  d.save.earned = 100;
  assert.ok(d.canBuy('jumps') && d.canBuy('magnet') && d.canBuyAny());
  assert.strictEqual(d.buy('jumps'), true);
  assert.strictEqual(d.upLevel('jumps'), 1); assert.strictEqual(d.save.spent, 80); assert.strictEqual(d.coins(), 20);
  assert.strictEqual(d.maxJumps(), 5); assert.strictEqual(d.upPrice('jumps'), 300);
  assert.deepStrictEqual(JSON.parse(g.store.get('teft_save')).up, { jumps: 1, magnet: 0 }, 'покупка сохранена сразу');
  assert.ok(!d.canBuy('jumps'), '20 монет на 300 не хватает'); assert.ok(!d.canBuy('magnet'), '20 < 60');
  assert.strictEqual(d.buy('magnet'), false, 'отказ ничего не меняет'); assert.strictEqual(d.save.spent, 80);
  d.save.earned = 2000;
  assert.strictEqual(d.buy('magnet'), true); assert.strictEqual(d.magnetRadius(), 60);
  assert.strictEqual(d.buy('magnet'), true); assert.strictEqual(d.magnetRadius(), 90);
  assert.strictEqual(d.buy('magnet'), true); assert.strictEqual(d.magnetRadius(), 120);
  assert.strictEqual(d.upPrice('magnet'), null); assert.strictEqual(d.buy('magnet'), false, 'на максимуме покупки нет');
  assert.strictEqual(d.buy('jumps'), true); assert.strictEqual(d.maxJumps(), 6); assert.strictEqual(d.buy('jumps'), false);
  assert.strictEqual(d.save.spent, 80 + 60 + 180 + 400 + 300); assert.ok(!d.canBuyAny(), 'всё куплено');
  assert.strictEqual(d.upValue('jumps', 0), 4); assert.strictEqual(d.upValue('magnet', 0), 0); assert.strictEqual(d.upValue('magnet', 2), 90);
  // языки: турецкий, запасная цепочка tr/de → en → ru
  d.setLang('tr'); assert.strictEqual(d.lang, 'tr'); assert.strictEqual(d.T('shop'), 'Mağaza'); assert.strictEqual(d.T('again'), 'Tekrar');
  d.setLang('de'); assert.strictEqual(d.lang, 'en'); assert.strictEqual(d.T('shop'), 'Shop');
  d.setLang('ru'); assert.strictEqual(d.T('up.jumps'), 'Заряды прыжка'); assert.strictEqual(d.T('buy'), 'Купить');
  console.log('test_upgrades ok');
})().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_upgrades.js`
Expected: FAIL — `AssertionError` на `d.JUMPS_BASE` (`undefined !== 4`).

- [ ] **Step 3: Написать src/upgrades.js**

```js
'use strict';
// ---------- апгрейды: каталог, цены, эффекты, покупка ----------
// Экономика этапа 2 черновая: цены правятся только здесь, по метрикам после релиза (spec 2026-09-23 §4).
const JUMPS_BASE = 4;
const UPGRADES = {
  jumps:  { levels: [{ price: 80, value: 5 }, { price: 300, value: 6 }] },                              // value — максимум зарядов
  magnet: { levels: [{ price: 60, value: 60 }, { price: 180, value: 90 }, { price: 400, value: 120 }] }, // value — радиус, px от края тефтели
};
function upLevel(id) { return (save.up && save.up[id]) || 0; }
function upMaxLevel(id) { return UPGRADES[id].levels.length; }
// значение эффекта на уровне level (0 — без апгрейда)
function upValue(id, level) { return level > 0 ? UPGRADES[id].levels[level - 1].value : (id === 'jumps' ? JUMPS_BASE : 0); }
function upPrice(id) { const l = upLevel(id); return l < upMaxLevel(id) ? UPGRADES[id].levels[l].price : null; } // null — максимум
function maxJumps() { return upValue('jumps', upLevel('jumps')); }
function magnetRadius() { return upValue('magnet', upLevel('magnet')); }
function canBuy(id) { const p = upPrice(id); return p !== null && coins() >= p; }
function canBuyAny() { return Object.keys(UPGRADES).some(canBuy); }
function buy(id) {
  if (!canBuy(id)) return false;
  save.spent += upPrice(id); save.up[id] = upLevel(id) + 1;
  persist(); sfx.buy();
  return true;
}
expose({ UPGRADES, JUMPS_BASE, upLevel, upMaxLevel, upValue, upPrice, maxJumps, magnetRadius, canBuy, canBuyAny, buy });
```

- [ ] **Step 4: Переписать src/i18n.js (три языка, цепочка запасных)**

```js
'use strict';
// ---------- i18n: ru, tr, остальные языки → en; недостающий ключ ищется в en, потом в ru ----------
const STR = {
  ru: {
    title: 'Тефтеля',
    hint1: 'Тап — прыжок.', hint2: 'Тап слева или справа — в ту сторону.', hint3: 'Ешь еду, расти. Волосы и грязь — не ешь.',
    hint4: 'Каждый прыжок тратит заряд, еда возвращает.', hint5: 'Об стену на скорости — теряешь мясо.',
    tapToJump: 'Тапни, чтобы прыгнуть',
    fell: 'Упала', best: 'лучший', runCoins: 'Собрано за забег', total: 'Всего',
    again: 'Ещё раз', continueAd: 'Продолжить', doubleAd: 'Монеты ×2', forAd: 'за рекламу',
    paused: 'Пауза', tapToContinue: 'Тапни, чтобы продолжить',
    mass: 'масса', hairProof: 'волосы не страшны', meatLost: '−1 мясо', pf: 'пф', m: 'м',
    adStub: 'Реклама (заглушка)', loading: 'Загрузка…',
    'item.hair': 'волос', 'item.dirt': 'грязь', 'item.fly': 'муха',
    shop: 'Магазин', back: 'Назад', buy: 'Купить', max: 'Макс', none: 'нет',
    'up.jumps': 'Заряды прыжка', 'up.magnet': 'Магнит', 'up.jumps.effect': 'Прыжков', 'up.magnet.effect': 'Радиус',
  },
  en: {
    title: 'Meatball',
    hint1: 'Tap to jump.', hint2: 'Tap left or right to jump that way.', hint3: 'Eat food to grow. Avoid hair and dirt.',
    hint4: 'Each jump costs a charge, food refills.', hint5: 'Hit a wall fast — lose meat.',
    tapToJump: 'Tap to jump',
    fell: 'Fell', best: 'best', runCoins: 'Coins this run', total: 'Total',
    again: 'Again', continueAd: 'Continue', doubleAd: 'Coins ×2', forAd: 'watch ad',
    paused: 'Paused', tapToContinue: 'Tap to continue',
    mass: 'mass', hairProof: 'hair-proof', meatLost: '−1 meat', pf: 'pff', m: 'm',
    adStub: 'Ad (stub)', loading: 'Loading…',
    'item.hair': 'hair', 'item.dirt': 'dirt', 'item.fly': 'fly',
    shop: 'Shop', back: 'Back', buy: 'Buy', max: 'Max', none: 'none',
    'up.jumps': 'Jump charges', 'up.magnet': 'Magnet', 'up.jumps.effect': 'Jumps', 'up.magnet.effect': 'Radius',
  },
  tr: {
    title: 'Köfte',
    hint1: 'Zıplamak için dokun.', hint2: 'Sola veya sağa dokun — o yöne zıpla.', hint3: 'Yemek ye, büyü. Kıl ve kirden kaçın.',
    hint4: 'Her zıplama bir şarj harcar, yemek doldurur.', hint5: 'Duvara hızla çarparsan et kaybedersin.',
    tapToJump: 'Zıplamak için dokun',
    fell: 'Düştü', best: 'en iyi', runCoins: 'Bu turda toplanan', total: 'Toplam',
    again: 'Tekrar', continueAd: 'Devam et', doubleAd: 'Para ×2', forAd: 'reklam izle',
    paused: 'Duraklatıldı', tapToContinue: 'Devam etmek için dokun',
    mass: 'kütle', hairProof: 'kıl işlemez', meatLost: '−1 et', pf: 'pöf', m: 'm',
    adStub: 'Reklam (taslak)', loading: 'Yükleniyor…',
    'item.hair': 'kıl', 'item.dirt': 'kir', 'item.fly': 'sinek',
    shop: 'Mağaza', back: 'Geri', buy: 'Satın al', max: 'Maks', none: 'yok',
    'up.jumps': 'Zıplama şarjı', 'up.magnet': 'Mıknatıs', 'up.jumps.effect': 'Zıplama', 'up.magnet.effect': 'Yarıçap',
  },
};
function langFor(code) { const c = String(code || '').slice(0, 2).toLowerCase(); return c === 'ru' ? 'ru' : c === 'tr' ? 'tr' : 'en'; }
// до YG.init()/setLang язык берём из браузера, чтобы «Загрузка…» не всегда была русской
let LANG = langFor((window.navigator && window.navigator.language) || 'ru');
function setLang(code) { LANG = langFor(code); }
function T(key) {
  for (const tbl of [STR[LANG], STR.en, STR.ru]) { const t = tbl[key]; if (t !== undefined) return t; }
  return key;
}
expose({ setLang, T, get lang() { return LANG; } });
```

- [ ] **Step 5: Звук покупки, список скриптов, npm test, поправка test_core**

`src/audio.js`, объект `sfx` — добавить строку после `die`:
```js
  buy:  () => { tone(600, 900, 0.08, 'triangle', 0.18); tone(900, 1300, 0.1, 'triangle', 0.15); },
```
`index.html`: после `<script src="src/save.js"></script>` вставить `<script src="src/upgrades.js"></script>`.
`package.json`, скрипт `test`: `node tools/test_core.js && node tools/test_sdk.js && node tools/test_save.js && node tools/test_upgrades.js && node tools/test_game.js && node tools/test_render.js && node tools/smoke.js`.
`tools/test_core.js`: турецкий теперь свой язык, поэтому строку `d.setLang('tr'); assert.strictEqual(d.T('again'), 'Again'); assert.strictEqual(d.lang, 'en', 'хук lang живой, не снимок');` заменить на `d.setLang('de'); assert.strictEqual(d.T('again'), 'Again'); assert.strictEqual(d.lang, 'en', 'хук lang живой, не снимок');`.

- [ ] **Step 6: Запустить тесты, убедиться, что проходят**

Run: `npm test`
Expected: семь строк `… ok` (core, sdk, save, upgrades, game, render, smoke), без предупреждений `_env: нет файла`.

- [ ] **Step 7: Коммит**

```bash
git add src/upgrades.js src/i18n.js src/audio.js index.html package.json tools/test_upgrades.js tools/test_core.js
git commit -m "feat: модуль апгрейдов (заряды, магнит), строки магазина, турецкий язык, звук покупки

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 3: Правила забега — гейты мусора, злой множитель, заряды до 6, магнит

**Files:**
- Modify: `src/world.js` (`TRASH_FROM`, `trashPoolFor`, `spawn`, `expose`), `src/game.js` (`COIN_MULT`, `coinMult`, `MAXJ` → `maxJumps()`, магнит, `expose`), `src/render.js` (`drawHUD`, иконки зарядов)
- Test: `tools/test_game.js` (дополнить)

**Interfaces:**
- Consumes: `maxJumps(), magnetRadius()` (upgrades.js), `save.earned`, `coins()` (save.js).
- Produces: `TRASH_FROM`, `trashPoolFor(h) → подмножество TRASH`, `COIN_MULT`, `MAGNET_SPEED = 300`, `coinMult()`; DBG добавляет `trashPoolFor, coinMult`. Константа `MAXJ` удаляется отовсюду.

- [ ] **Step 1: Дописать тесты в tools/test_game.js** (перед строкой `// reset сбрасывает флаги забега`)

```js
  // множитель: ×1.5 / ×2 / ×3 / ×4 с массы 4 / 7 / 10 / 14
  for (const [m, k] of [[1, 1], [3, 1], [4, 1.5], [6, 1.5], [7, 2], [9, 2], [10, 3], [13, 3], [14, 4], [20, 4]]) { ball.mass = m; assert.strictEqual(d.coinMult(), k, 'масса ' + m); }
  // гейты мусора: до 25 м только еда, 25–60 только волосы, 60–100 без мухи
  assert.deepStrictEqual(Object.keys(d.trashPoolFor(10)), []);
  assert.deepStrictEqual(Object.keys(d.trashPoolFor(30)), ['hair']);
  assert.deepStrictEqual(Object.keys(d.trashPoolFor(70)), ['hair', 'dirt']);
  assert.deepStrictEqual(Object.keys(d.trashPoolFor(150)), ['hair', 'dirt', 'fly']);
  { const all = []; for (let i = 0; i < 30; i++) { d.reset(); d.state = 'play'; d.update(0.016); all.push(...d.items); }
    const hOf = it => -it.y / 10; // предмет может лежать на ±2.5 м от строки спавна, поэтому запас 3 м
    assert.ok(all.some(it => it.trash), 'мусор вообще спавнится');
    assert.ok(!all.some(it => it.trash && hOf(it) < 22), 'ниже 25 м мусора нет');
    assert.ok(!all.some(it => it.trash && it.kind !== 'hair' && hOf(it) < 57), 'ниже 60 м только волосы');
    assert.ok(!all.some(it => it.kind === 'fly' && hOf(it) < 97), 'ниже 100 м мухи нет'); }
  // заряды по апгрейду: максимум 6, еда и регенерация наполняют до него, «Продолжить» даёт полный
  d.save.up.jumps = 2; d.reset(); d.state = 'play';
  assert.strictEqual(ball.jumps, 6); d.jump(1); d.jump(1); d.jump(1);
  d.items.push(item(d, 'meat', false, ball.x, ball.y)); d.update(0.016); assert.strictEqual(ball.jumps, 6, 'еда наполняет до 6');
  ball.x = 240; ball.y = 0; ball.vx = 0; ball.vy = 0; // вернуть на тарелку: после трёх прыжков вбок тефтеля улетела бы со сцены и погибла до конца регенерации
  ball.jumps = 5; for (let i = 0; i < 110; i++) d.update(0.016); assert.strictEqual(d.state, 'play'); assert.strictEqual(ball.jumps, 6, 'регенерация до 6');
  d.die(); d.continueRun(); assert.strictEqual(ball.jumps, 6);
  d.save.up.jumps = 0; d.reset(); assert.strictEqual(ball.jumps, 4);
  // магнит: еда в радиусе подтягивается, мусор и еда вне радиуса — нет
  d.save.up.magnet = 1; d.reset(); d.state = 'play';
  const near = item(d, 'ketchup', false, ball.x + ball.r + 50, ball.y), far = item(d, 'ketchup', false, ball.x + ball.r + 200, ball.y), junk = item(d, 'hair', true, ball.x - ball.r - 50, ball.y);
  d.items.push(near, far, junk); const nx = near.x, fx = far.x, jx = junk.x; d.update(0.016);
  assert.ok(near.x < nx - 3 && !near.dead, 'еда в радиусе подтянулась и ещё не съедена');
  assert.strictEqual(far.x, fx, 'еда вне радиуса на месте'); assert.strictEqual(junk.x, jx, 'мусор не притягивается');
  d.save.up.magnet = 0; d.reset(); d.state = 'play';
  const n2 = item(d, 'ketchup', false, ball.x + ball.r + 50, ball.y); d.items.push(n2); const n2x = n2.x; d.update(0.016);
  assert.strictEqual(n2.x, n2x, 'без апгрейда магнита нет');
  // смерть и ×2 пишут в earned
  d.reset(); d.state = 'play'; const e0 = d.save.earned; d.setRunCoins(7); d.die(); assert.strictEqual(d.save.earned, e0 + 7);
  d.doubleCoins(); assert.strictEqual(d.save.earned, e0 + 14);
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_game.js`
Expected: FAIL — `TypeError: d.coinMult is not a function`.

- [ ] **Step 3: Правки src/world.js**

После объявления `TRASH` добавить:
```js
// онбординг: каждый вид мусора появляется со своей высоты (м), чтобы игрок знакомился с угрозами по одной
const TRASH_FROM = { hair: 25, dirt: 60, fly: 100 };
function trashPoolFor(h) { const pool = {}; for (const k of Object.keys(TRASH)) if (h >= TRASH_FROM[k]) pool[k] = TRASH[k]; return pool; }
```
В `spawn()` заменить три строки
```js
      const isTrash = h > 8 && Math.random() < trashP;
      const key = isTrash ? pick(TRASH) : pick(FOOD);
      const def = isTrash ? TRASH[key] : FOOD[key];
```
на
```js
      const pool = trashPoolFor(h);
      const isTrash = Object.keys(pool).length > 0 && Math.random() < trashP;
      const key = isTrash ? pick(pool) : pick(FOOD);
      const def = isTrash ? TRASH[key] : FOOD[key];
```
Последняя строка файла: `expose({ FOOD, TRASH, TRASH_FROM, trashPoolFor, get camY() { return camY; }, get items() { return items; }, get plates() { return plates; }, popText });`

- [ ] **Step 4: Правки src/game.js**

- Удалить `const MAXJ = 4; …`; вместо неё после `G`:
```js
const COIN_MULT = [[14, 4], [10, 3], [7, 2], [4, 1.5]]; // [масса, множитель монет] — первая подходящая ступень сверху вниз
const MAGNET_SPEED = 300;       // px/с — с какой скоростью магнит подтягивает еду
```
- `let state = 'title';            // title | play | dead | shop`
- `function coinMult() { for (const [m, k] of COIN_MULT) if (ball.mass >= m) return k; return 1; }`
- `reset()`: `ball.jumps = maxJumps();`
- `eat()`, ветка еды:
```js
    it.dead = true;
    const mult = coinMult(), gain = Math.round(d.coins * mult); // множитель до прироста массы — и для монет, и для флага big
    ball.mass += d.mass; runCoins += gain;
    ball.jumps = Math.min(maxJumps(), ball.jumps + d.jumps);
    ball.mouth = 1;
    pulse(140 * d.mass);
    burst(it.x, it.y, colorOf(it.kind), 10 + d.mass * 6, 260, 0.5, 5);
    popText(it.x, it.y - 20, '+' + gain, '#ffe08a', d.mass > 1 || mult > 1);
```
- `continueRun()`: `ball.jumps = maxJumps();`
- `updateRun()`: регенерация `if (ball.jumps < maxJumps()) { … }`; перед циклом по предметам `const mr = magnetRadius();`, а в цикле после `if (it.dead) continue;` и до движения мухи:
```js
    if (mr > 0 && !it.trash) { // магнит: еда в радиусе подтягивается к центру, мусор нет
      const dx0 = ball.x - it.x, dy0 = ball.y - it.y, dist = Math.hypot(dx0, dy0);
      if (dist > 0 && dist < ball.r + mr) { const step = Math.min(dist, MAGNET_SPEED * dt); it.x += dx0 / dist * step; it.y += dy0 / dist * step; }
    }
```
- `expose`: добавить `coinMult` в список (`reset, jump, die, continueRun, doubleCoins, update, coinMult,`).

- [ ] **Step 5: Правка src/render.js (HUD)**

Цикл иконок зарядов:
```js
  const mj = maxJumps();
  for (let i = 0; i < mj; i++) { // заряды прыжка — столько, сколько максимум с апгрейдами
    const x = W / 2 - (mj - 1) * 14 + i * 28, y = 40;
```
(остальное тело цикла без изменений).

- [ ] **Step 6: Запустить тесты, убедиться, что проходят**

Run: `npm test`
Expected: семь `… ok`. `grep -rn MAXJ src/` — пусто.

- [ ] **Step 7: Коммит**

```bash
git add src/world.js src/game.js src/render.js tools/test_game.js
git commit -m "feat: гейты мусора 25/60/100 м, множитель ×1.5–×4, заряды до 6 по апгрейду, магнит

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 4: Экран магазина, кнопки входа, состояние `shop`

**Files:**
- Modify: `src/screens.js` (`button` со стилем, `badge`, `titleScreen`, `resultsScreen`, `shopScreen`, `flashCard`, `upEffectText`, `expose`), `src/main.js` (`shopReturn`, `openShop/closeShop`, `onTap`, клавиши, `draw`, `expose`)
- Test: `tools/test_render.js`, `tools/smoke.js` (дополнить)

**Interfaces:**
- Consumes: `UPGRADES, upLevel, upMaxLevel, upValue, upPrice, canBuy, canBuyAny, buy` (upgrades.js), `coins()` (save.js), `tGame` (world.js), `T` (i18n.js).
- Produces: `button(id, x, y, w, h, label, sub, ad, style)` где `style` ∈ `undefined | 'neutral' | 'dim'`; объекты кнопок получают `badge: true`, если нарисована точка; `shopScreen()`, `flashCard(id)`, `upEffectText(id) → string`; `openShop(from)`, `closeShop()`; кнопки `shop` на титуле (`H*0.62`) и результатах (4-я), `buy:jumps`, `buy:magnet`, `back`. DBG: `shopScreen, flashCard, upEffectText, openShop, closeShop, get shopReturn`.

- [ ] **Step 1: Дописать тесты**

`tools/test_render.js` — сначала обновить четыре старые проверки списка кнопок результатов (кнопка `shop` теперь всегда последняя):
`['continue', 'double', 'again']` → `['continue', 'double', 'again', 'shop']`; `['continue', 'again']` (после `doubleCoins`) → `['continue', 'again', 'shop']`;
`['again']` → `['again', 'shop']`; `['continue', 'again'], '×2 только от 10 монет'` → `['continue', 'again', 'shop'], '×2 только от 10 монет'`.
Затем перед `console.log('test_render ok');` добавить:
```js
  // магазин: кнопки, тексты эффектов, бейдж и покупка
  d.setLang('ru'); d.save.earned = 0; d.save.spent = 0; d.reset(); d.state = 'title'; texts.length = 0; d.titleScreen();
  let sb = d.buttons.find(b => b.id === 'shop'); assert.ok(sb, 'кнопка Магазин на титуле'); assert.ok(!sb.badge, 'без монет точки нет');
  assert.ok(texts.includes('Магазин'));
  d.state = 'play'; d.setRunCoins(20); d.die(); d.resultsScreen(true);
  assert.deepStrictEqual(d.buttons.map(b => b.id), ['continue', 'double', 'again', 'shop'], 'Магазин — четвёртая кнопка');
  d.save.earned = 500; d.resultsScreen(true); sb = d.buttons.find(b => b.id === 'shop'); assert.ok(sb.badge, 'монет хватает — точка');
  texts.length = 0; d.shopScreen();
  assert.deepStrictEqual(d.buttons.map(b => b.id), ['buy:jumps', 'buy:magnet', 'back']);
  assert.ok(texts.includes('Прыжков: 4 → 5') && texts.includes('Радиус: нет → 60') && texts.includes('Купить 80') && texts.includes('Купить 60'), 'эффекты и цены');
  assert.strictEqual(d.upEffectText('jumps'), 'Прыжков: 4 → 5');
  assert.ok(d.buy('magnet')); d.flashCard('magnet'); texts.length = 0; d.shopScreen();
  assert.ok(texts.includes('Радиус: 60 → 90') && texts.includes('Купить 180'));
  d.save.spent = 0; d.save.up.jumps = 2; texts.length = 0; d.shopScreen(); assert.ok(texts.includes('Прыжков: 6') && texts.includes('Макс'), 'на максимуме — Макс');
  texts.length = 0; d.setLang('en'); d.shopScreen(); assert.ok(texts.includes('Shop') && texts.includes('Back'));
  texts.length = 0; d.setLang('tr'); d.shopScreen(); assert.ok(texts.includes('Mağaza') && texts.includes('Geri'));
  d.setLang('ru');
```
`tools/smoke.js` — в `runFlow` перед строкой `// «Ещё раз»: первый рестарт сессии без рекламы`:
```js
  // магазин с экрана результатов: покупка пятого заряда, повторный тап без денег, Escape назад
  d.save.earned = d.save.spent + 100; g.step(); // ровно 100 монет: хватает на заряд (80), но не на второй уровень (300)
  const shopBtn = btn(d, 'shop'); assert.ok(shopBtn && shopBtn.badge, 'кнопка Магазин с точкой — монет хватает');
  g.tap(shopBtn.x, shopBtn.y); g.step(); assert.strictEqual(d.state, 'shop');
  const lenBeforeShop = d.YG.log.length;
  const buyJ = btn(d, 'buy:jumps'); assert.ok(buyJ, 'кнопка покупки зарядов');
  const spent0 = d.save.spent; g.tap(buyJ.x, buyJ.y); g.step();
  assert.strictEqual(d.upLevel('jumps'), 1); assert.strictEqual(d.save.spent, spent0 + 80);
  g.tap(buyJ.x, buyJ.y); g.step(); assert.strictEqual(d.upLevel('jumps'), 1, 'второй уровень стоит 300 — не хватает');
  g.key('Escape'); g.step(); assert.strictEqual(d.state, 'dead', 'Escape возвращает на результаты');
  assert.strictEqual(d.YG.log.length, lenBeforeShop, 'магазин не трогает GameplayAPI');
```
и после `assert.strictEqual(d.state, 'play'); assert.strictEqual(inters(), n0, 'первый рестарт без рекламы');` добавить строку
`assert.strictEqual(d.ball.jumps, 5, 'после покупки в новом забеге 5 зарядов');`.
В финальном IIFE после блока «награды нет» добавить:
```js
  // магазин с титула: без монет точки нет, магазин не шлёт start, «Назад» возвращает, тап вне кнопки стартует
  { const g = require('./_env')(ctx); const d = g.dbg(); await g.boot(); g.step();
    const sb = btn(d, 'shop'); assert.ok(sb, 'кнопка Магазин на титуле'); assert.ok(!sb.badge, 'без монет точки нет');
    g.tap(sb.x, sb.y); g.step(); assert.strictEqual(d.state, 'shop'); assert.deepStrictEqual(d.YG.log, ['ready'], 'магазин не шлёт start');
    const back = btn(d, 'back'); g.tap(back.x, back.y); g.step(); assert.strictEqual(d.state, 'title');
    g.tap(240, 100); assert.strictEqual(d.state, 'play', 'тап вне кнопки стартует игру'); }
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `node tools/test_render.js`
Expected: FAIL — `AssertionError: кнопка Магазин на титуле`.

- [ ] **Step 3: Правки src/screens.js**

`button` с параметром стиля и бейдж:
```js
// style: undefined — жёлтая основная; 'neutral' — светлая второстепенная (Магазин, Назад); 'dim' — недоступная
function button(id, x, y, w, h, label, sub, ad, style) {
  const b = { id, x, y, w, h, label, sub, ad }; buttons.push(b);
  ctx.save();
  ctx.fillStyle = ad ? '#2e7d32' : style === 'neutral' ? '#ece6da' : style === 'dim' ? 'rgba(255,255,255,0.15)' : '#f6c343';
  rrect(x - w / 2, y - h / 2, w, h, 14); ctx.fill();
  ctx.fillStyle = ad ? '#fff' : style === 'dim' ? 'rgba(255,255,255,0.5)' : '#1b1410'; ctx.textAlign = 'center';
  if (sub) {
    ctx.font = '800 22px system-ui, sans-serif'; ctx.fillText((ad ? '▶ ' : '') + label, x, y - 2);
    ctx.font = '500 13px system-ui, sans-serif'; ctx.globalAlpha = 0.85; ctx.fillText(sub, x, y + 17);
  } else { ctx.font = '800 24px system-ui, sans-serif'; ctx.fillText(label, x, y + 9); }
  ctx.restore();
  return b;
}
function badge(b) { // красная точка у правого верхнего угла — «есть что купить»
  b.badge = true;
  ctx.fillStyle = '#e3342f'; ctx.beginPath(); ctx.arc(b.x + b.w / 2 - 6, b.y - b.h / 2 + 6, 6, 0, 7); ctx.fill();
}
```
`titleScreen()` — после подсказок и перед пульсирующей строкой:
```js
  const sb = button('shop', W / 2, H * 0.62, 220, 52, T('shop'), '', false, 'neutral'); if (canBuyAny()) badge(sb);
```
а строка `tapToJump` рисуется на `H * 0.74` вместо `H * 0.72`.
`resultsScreen()` — после `button('again', …)`:
```js
  y += 76; const sb = button('shop', W / 2, y, 300, 60, T('shop'), '', false, 'neutral'); if (canBuyAny()) badge(sb);
```
Новые функции (перед `pausedScreen`):
```js
let shopFlash = null; // { id, until } — подсветка рамки только что купленной карточки
function flashCard(id) { shopFlash = { id, until: tGame + 0.3 }; }
function upEffectText(id) { // «Прыжков: 4 → 5», «Радиус: нет → 60», на максимуме «Прыжков: 6»
  const lvl = upLevel(id), max = upMaxLevel(id);
  const fmt = v => (id === 'magnet' && v === 0) ? T('none') : String(v);
  const label = T('up.' + id + '.effect') + ': ' + fmt(upValue(id, lvl));
  return lvl >= max ? label : label + ' → ' + fmt(upValue(id, lvl + 1));
}
function shopScreen() {
  clearButtons(); dim(0.85);
  ctx.textAlign = 'center'; ctx.fillStyle = '#fff';
  ctx.font = '900 44px system-ui, sans-serif'; ctx.fillText(T('shop'), W / 2, H * 0.14);
  ctx.textAlign = 'right'; ctx.font = '700 22px system-ui, sans-serif'; ctx.fillStyle = '#ffe08a';
  ctx.fillText('● ' + coins(), W - 24, H * 0.14);
  Object.keys(UPGRADES).forEach((id, i) => {
    const cw = 400, ch = 150, x0 = W / 2 - cw / 2, y0 = H * (0.27 + i * 0.23) - ch / 2;
    const flash = shopFlash && shopFlash.id === id && tGame < shopFlash.until;
    ctx.fillStyle = 'rgba(255,255,255,0.08)'; rrect(x0, y0, cw, ch, 16); ctx.fill();
    ctx.strokeStyle = flash ? '#ffe08a' : 'rgba(255,255,255,0.18)'; ctx.lineWidth = flash ? 4 : 2; rrect(x0, y0, cw, ch, 16); ctx.stroke();
    ctx.textAlign = 'left'; ctx.fillStyle = '#fff'; ctx.font = '800 24px system-ui, sans-serif'; ctx.fillText(T('up.' + id), x0 + 20, y0 + 38);
    ctx.font = '500 18px system-ui, sans-serif'; ctx.fillStyle = 'rgba(255,255,255,0.85)'; ctx.fillText(upEffectText(id), x0 + 20, y0 + 68);
    const lvl = upLevel(id), max = upMaxLevel(id); // индикатор уровней
    for (let k = 0; k < max; k++) { ctx.fillStyle = k < lvl ? '#f6c343' : 'rgba(255,255,255,0.25)'; ctx.beginPath(); ctx.arc(x0 + 28 + k * 22, y0 + 112, 7, 0, 7); ctx.fill(); }
    const price = upPrice(id);
    if (price === null) button('buy:' + id, x0 + cw - 110, y0 + ch - 36, 200, 48, T('max'), '', false, 'dim');
    else button('buy:' + id, x0 + cw - 110, y0 + ch - 36, 200, 48, T('buy') + ' ' + price, '', false, canBuy(id) ? undefined : 'dim');
  });
  button('back', W / 2, H * 0.80, 220, 52, T('back'), '', false, 'neutral');
}
```
`expose({ titleScreen, resultsScreen, pausedScreen, adStubScreen, loadingScreen, shopScreen, flashCard, upEffectText, get buttons() { return buttons; }, hitButton });`

- [ ] **Step 4: Правки src/main.js**

- После `let restarts = …`: `let shopReturn = 'title'; // куда возвращаться из магазина: title | dead`
- Перед `// --- ввод ---`:
```js
// --- магазин: меню, GameplayAPI не трогаем ---
function openShop(from) { shopReturn = from; state = 'shop'; }
function closeShop() { state = shopReturn; }
```
- `onTap`: ветка титула становится
```js
  if (state === 'title') {
    const b = hitButton(x, y); if (b && b.id === 'shop') { openShop('title'); return; }
    state = 'play'; YG.gameplayStart(); jump(x < ball.x ? -1 : 1); return;
  }
```
в ветке `dead` после `else if (b.id === 'double') tryDouble();` добавить `else if (b.id === 'shop') openShop('dead');`; после ветки `dead` добавить
```js
  if (state === 'shop') {
    const b = hitButton(x, y); if (!b) return;
    if (b.id === 'back') closeShop();
    else if (b.id.startsWith('buy:')) { const id = b.id.slice(4); if (buy(id)) flashCard(id); }
    return;
  }
```
- Клавиатура:
```js
window.addEventListener('keydown', e => {
  if (e.repeat) return;
  const left = e.code === 'ArrowLeft' || e.code === 'KeyA', right = e.code === 'ArrowRight' || e.code === 'KeyD';
  const up = e.code === 'Space' || e.code === 'ArrowUp' || e.code === 'KeyW' || e.code === 'Enter', esc = e.code === 'Escape';
  if (!left && !right && !up && !esc) return;
  e.preventDefault();
  if (state === 'shop') { if (up || esc) closeShop(); return; }
  if (state === 'dead') { if (up) { const b = buttons.find(b => b.id === 'again'); if (b) onTap(b.x, b.y); } return; }
  if (esc) return;
  onTap(left ? ball.x - 10 : right ? ball.x + 10 : ball.x + (ball.vx >= 0 ? 10 : -10), 0);
});
```
- `draw()`: после `if (state === 'dead') …` добавить `if (state === 'shop') shopScreen();`
- `expose`: добавить `openShop, closeShop, get shopReturn() { return shopReturn; }` в первый `expose({...})`.

- [ ] **Step 5: Запустить тесты, убедиться, что проходят**

Run: `npm test`
Expected: семь `… ok`, `smoke ok`.

- [ ] **Step 6: Коммит**

```bash
git add src/screens.js src/main.js tools/test_render.js tools/smoke.js
git commit -m "feat: экран магазина, кнопки входа с бейджем, состояние shop, Escape

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

### Task 5: Скриншоты, README, журнал, сборка

**Files:**
- Modify: `tools/shot.js`, `README.md`, `docs/DEVLOG.md`

**Interfaces:**
- Consumes: DBG `openShop, buy, setRunCoins, die, save`; `opts.lang` в `_env`.
- Produces: `shots/shop.png`, `shots/en_shop.png`, `shots/tr_results.png`, обновлённый `shots/title.png`; README и DEVLOG, отражающие этап 2.

- [ ] **Step 1: Дополнить tools/shot.js**

После `const dead = …` добавить:
```js
// магазин после забега: один уровень магнита куплен, на пятый заряд хватает (жёлтая кнопка), на второй магнит — нет (серая)
const shop = async (g, d) => { await dead(g, d); d.save.earned += 150; d.buy('magnet'); d.openShop('dead'); g.step(); };
```
и в конец списка съёмок:
```js
  await shoot('shop', 480, 854, shop);
  await shoot('en_shop', 480, 854, shop, { lang: 'en-US' });
  await shoot('tr_results', 480, 854, dead, { lang: 'tr-TR' });
```

- [ ] **Step 2: Снять и посмотреть**

Run: `node tools/shot.js`
Expected: девять строк `shots/*.png`. Посмотреть глазами (Read на PNG):
- `title.png`: между подсказками и «Тапни, чтобы прыгнуть» светлая кнопка «Магазин» без точки; тефтеля на тарелке не перекрыта.
- `shop.png`: «Магазин», баланс справа; карточка «Заряды прыжка» с «Прыжков: 4 → 5», две точки (пустые), жёлтая «Купить 80»; карточка «Магнит» с «Радиус: 60 → 90», одна из трёх точек заполнена, серая «Купить 180»; внизу «Назад»; ничего не перекрывается.
- `en_shop.png`: «Shop», «Jump charges», «Jumps: 4 → 5», «Buy 80», «Magnet», «Radius: 60 → 90», «Back».
- `tr_results.png`: «Düştü», «Devam et / reklam izle», «Para ×2 / reklam izle», «Tekrar», «Mağaza».
- `results.png`: четыре кнопки, нижняя «Магазин» помещается в экран.
Расхождение — описать в отчёте, не править рендер молча.

- [ ] **Step 3: README**

Обновить разделы (полные замены абзацев):
- «Правила», пункт про заряды: `- **Прыжки — ресурс**: 4 заряда (\`JUMPS_BASE\`), апгрейд поднимает до 5 и 6 (\`maxJumps()\`). Каждый прыжок тратит заряд. Еда возвращает: кетчуп +1, макароны +2, мясо +3. Без еды заряды копятся сами по одному за 1.6 с (\`REGEN\`).`
- Пункт про мусор дополнить: `Мусор появляется по высоте (\`TRASH_FROM\`): волосы с 25 м, грязь с 60 м, муха со 100 м; до 25 м только еда.`
- Пункт «Масса даёт»: `множитель монет (\`COIN_MULT\`: масса 4 → ×1.5, 7 → ×2, 10 → ×3, 14 → ×4)`.
- Новый пункт: `- **Магазин** (\`src/upgrades.js\`, вход с титула и с результатов): заряды 80 и 300 монет, магнит 60/180/400 монет с радиусом 60/90/120 px (\`magnetRadius()\`, тянет только еду). Экономика черновая, цены только в \`UPGRADES\`.`
- «Устройство кода», таблица: строка `src/upgrades.js` («каталог апгрейдов, цены, \`maxJumps()\`, \`magnetRadius()\`, \`buy()\`») после `src/save.js`; строка `src/save.js` → «\`save {v:2, best, earned, spent, up}\`, \`coins()\` = earned − spent; облако + localStorage, при загрузке максимум по полям (все поля монотонны)»; строка `src/i18n.js` → «строки ru/en/tr, \`T(key)\`; \`ru\` → русский, \`tr\` → турецкий, всё остальное → английский».
- «Как проверять»: `npm test` — «семь тестовых скриптов и smoke».
- «План этапов»: `2. ✅ Мета-слой — \`docs/specs/2026-09-23-meta-shop-design.md\`.`
- «Открытые вопросы к дизайнеру»: удалить три закрытых; оставить/добавить: `- Вертикальные полосы на телефонах 19.5:9: расширять мир или затемнять как бока?` и `- Цены магазина — черновые, настроить по метрикам после релиза.`

- [ ] **Step 4: DEVLOG**

Заменить блок «Текущий статус»:
```markdown
## Текущий статус

- Этап 2 «Мета-слой» реализован: магазин (заряды до 6, магнит 3 уровня), гейты мусора 25/60/100 м, множитель ×1.5–×4,
  сохранения v2 (earned/spent), турецкий язык. `npm test` / `npm run shot` / `npm run build` зелёные.
- SDK живьём по-прежнему не проверялся — ждём черновик игры в консоли от владельца (`docs/CHECKLIST-yandex-console.md`).
- Экономика и графика черновые; по каждой теме будет отдельный проход. Следующий этап — 3, этажи башни.
```
Вставить под вводным абзацем новую запись (выше «2026-09-22 — Сессия 2»):
```markdown
## 2026-09-23 — Сессия 3: этап 2, мета-слой

**Решено**
- Щадящий старт через гейты мусора (25/60/100 м, по одному виду), старт с массы 1. Причина: обучение через игру, без текста.
- Заряды продаются (5-й за 80, 6-й за 300), максимум 6 — самая ощутимая первая покупка.
- Множитель злее: ×1.5/×2/×3/×4 с массы 4/7/10/14 — дилемма «расти или беречься» должна быть острой; инфляцию гасят цены.
- Каталог только заряды + магнит (5 покупок). Стартовая масса, броня, +монеты отвергнуты владельцем.
- Магазин отдельным экраном с входом с титула и результатов; бейдж, когда есть что купить.
- Сохранения v2: монотонный журнал earned/spent + уровни; слияние по максимуму остаётся корректным.
- Турецкий язык (крупный рынок Яндекс Игр); `T()` ищет в текущем → en → ru.
- Экономика черновая — все числа подлежат настройке по метрикам.

**Отвергнуто**
- Стартовая масса 2, броня, «+% монет» — на первой версии меты лишнее.
- Магазин прямо на экране результатов — тесно на телефонах.
- Last-writer-wins и «облако главное» для сохранений — теряют прогресс анонимов и старшего устройства.

**Сделано**
- Задачи 1–5 плана `docs/plans/2026-09-23-meta-shop.md`.

**Дальше**
- Этап 3: этажи башни; владелец — черновик в консоли и живой тест SDK.

**Открытые вопросы**
- Вертикальные полосы на телефонах 19.5:9. Цены магазина по метрикам.
```

- [ ] **Step 5: Финальная проверка и коммит**

Run: `npm test && npm run build && git status --short`
Expected: семь `… ok`, `smoke ok (dist)`, размеры; в статусе только shot.js, README, DEVLOG.

```bash
git add tools/shot.js README.md docs/DEVLOG.md
git commit -m "docs: скриншоты магазина и турецкого, README и журнал под этап 2

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
